"""Microsoft Graph connector for FrostGate field assessments.

Read-only, bounded, cryptographically audited connector that produces
deterministic ScanResult objects from Microsoft Graph data.

Not a standalone tool — feeds scan_results consumed by the FrostGate
report generator, scoring engine, and client portal.
"""
