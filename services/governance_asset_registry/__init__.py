"""Governance Asset Registry — single source of truth for all AI assets.

Provides cryptographically verified, temporally versioned, attestation-gated
governance over every AI system, vendor, model, agent, OAuth app, automation,
and data flow in a tenant's inventory.

Trust-but-Verify: every declared fact is backed by a signed canonical hash;
the audit chain is tamper-evident; risk scores are deterministic and reproducible.
"""
