__pycache__/
*.pyc
.env
.venv/
ENV/
build/
dist/
node_modules/
.DS_Store
.idea/
.vscode/
coverage/
pytest_cache/
supervisor-sidecar/supervisor-sidecar

supervisor-sidecar/supervisor-sidecar
# Python
.venv/
__pycache__/
*.pyc

# Env / secrets
.env
.env.*
*.env

# Local DB / volumes / state
fg_pgdata/
*.sqlite
*.db

# Logs
*.log
logs/
.env
.env
# local/debug artifacts
*.bak
*.bak.*
*.log
*.tmp

# accidental json dumps / exports (only where they belong)
artifacts/**/*.json
state/**/*.json
agent_queue/**/*.json
!contracts/artifacts/
!contracts/artifacts/*.json


# local state
state/
*.sqlite*
__pycache__/
.pytest_cache/
.venv/
# ---- FrostGate runtime/state ----
state/
*.log

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
.venv*/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- Local DB artifacts (never commit) ----
*.db
*.sqlite
*.sqlite3
*.db-wal
*.db-shm
*.db-journal
*.sqlite-wal
*.sqlite-shm

# ---- OS / editor ----
.DS_Store
.vscode/
.idea/

# FrostGate secrets/state
secrets/
!secrets/*.example.txt
state/
.env
.env.*
!.env.example
state/

# junk files accidentally created by bad paste/output redirection
=
CACHED
exporting
importing
loading
reading
resolve
resolving
sending
transferring

# Local patch/backups
*.bak
*.bak.*
*.orig
*.rej

# local runtime
state/
artifacts/
.pytest_cache/
__pycache__/
*.db
# signing keys (never commit)
minisign.key
minisign.pub
CONTEXT_SNAPSHOT.md
scripts/_scratch/
scripts/_scratch/*
api/_scratch/

# local diagnostics (do not commit)
scripts/diag_*.py
tests/diag_*.py
tests/test__something.py

# runtime queue artifacts
agent_queue/
agent_queue/**/*.json

# snapshots / local reports
CONTEXT_SNAPSHOT.md
frostgate_context_snapshot.txt
frostgate_tree*.txt

# keys / signing
keys/
minisign.key
minisign.pub

# compiled binaries (unless intentionally committed)
supervisor-sidecar/supervisor-sidecar

# ---- Runtime queues / artifacts (never commit) ----
agent_queue/
agent_queue/**/*.json

# ---- Local snapshots / reports (never commit) ----
CONTEXT_SNAPSHOT.md
frostgate_context_snapshot.txt
frostgate_tree*.txt

# ---- Keys (never commit) ----
keys/

# ---- Built binaries (never commit; build in CI) ----
supervisor-sidecar/supervisor-sidecar

# ---- Contracted artifacts (versioned schemas) ----
!contracts/artifacts/
!contracts/artifacts/*.json
