version: "3.9"
services:
  agent:
    build:
      context: ..
      dockerfile: agent/Dockerfile
    env_file:
      - .env
    volumes:
      - ./data:/data
    depends_on:
      - redis
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
