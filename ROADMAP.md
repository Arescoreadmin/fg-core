# FrostGate Roadmap

**Owner:** Jason Cosat  
**Goal:** Client-ready field assessment delivery within 30 days of 2026-05-27  
**Authority:** `SYSTEM.md` (system design), `BLUEPRINT_STAGED.md` (governance compliance gates)

---

## How to use this document

Every PR that ships a feature, fixes a product gap, or changes the client-facing story must be listed here.  
Update the relevant section when a PR merges — do not backfill multiple PRs at once.  
"Not standalone" modules (field assessment, connectors, portal) require this note in their PR description:
> This module is not standalone. It requires the fg-core API, auth layer, and Postgres substrate.

---

## Phase 0 — Infrastructure Foundation
*30-day repo blitz: tenant isolation, auth boundary, CI stability, observability, agent packaging.*  
Tracking: `plans/30_day_repo_blitz.yaml` + `plans/30_day_repo_blitz.state.yaml` (complete as of PR 18.6)

All Phase 0 tasks are complete (tasks 1.1 – 18.6).

---

## Phase 1 — Field Assessment Layer
*Build the assessment-to-report pipeline: scan ingestion, findings, governance assets, report engine, portal.*

| PR | Title | Status | Key deliverable |
|----|-------|--------|----------------|
| PR 1 | FA Substrate | ✅ merged | Engagements, scan results, findings DB + API |
| PR 2 | FA Evidence & Playbooks | ✅ merged | Evidence anchoring, field observations, playbook engine |
| PR 3 | FA Connector Framework | ✅ merged | Connector registry, driver interface, scan dispatch |
| PR 4 | FA Governance Asset Layer | ✅ merged | Asset registry, attestation records, continuity tracking |
| PR 5 | FA Report Engine | ✅ merged | Report compilation, section hashes, manifest signing |
| PR 6 | FA Drift Detection | ✅ merged | Config drift detection, baseline snapshots, delta alerts |
| PR 7 | FA Console — Engagements | ✅ merged | Console UI: engagement list, finding list, report list |
| PR 8 | FA Console — Report Viewer | ✅ merged | Console UI: report viewer with finding expand, evidence lineage |
| PR 9 | FA Console — Governance | ✅ merged | Console UI: asset registry, attestation submit, continuity gaps |
| PR 10 | FA Portal — Client View | ✅ merged | Portal: client-facing engagement list + finding summary |
| PR 11 | FA Portal — Attestation | ✅ merged | Portal: attestation submission + health dashboard |
| PR 12 | FA MS Graph Connector | ✅ merged | MS Graph MSAL device-code scan (MFA, NIST AI RMF controls) |
| PR 13 | CI Budget Extension | ✅ merged | fg-fast 360s→480s, Guard timeout 15→20min |
| PR 14 | Dependency Authority | ✅ merged | Shared base requirements normalization |
| PR 15 | Report Engine Completion | ✅ merged | Full report engine: normalized findings, framework summary |
| PR 16 | Auth Runtime Guard + Key Store | ✅ merged | Persistent SQLite key store, auth runtime guard |
| PR 17 | Postgres Auth Authority | ✅ merged | Migrate auth to Postgres, multi-worker safe |
| PR 18 | FA Portal — Reports | ✅ merged | Portal: report version list, report viewer, export |
| PR 19 | FA Portal — Findings | ✅ merged | Portal: finding list with severity filter |
| PR 20 | FA Portal — Continuity | ✅ merged | Portal: continuity gap view, overdue alerts |
| PR 21 | FA Portal — Bug Fixes | ✅ merged | Portal attestation/continuity UI fixes + CI pass |
| PR 22 | Finding Explainer | ✅ merged (#390) | Plain-language finding explanations, LRU cache, provenance manifest |
| PR 27 | Executive Summary | ✅ merged | AI-generated narrative section in report; risk posture + key concerns; console + portal |
| PR 28 | NIST Control Coverage Matrix | ✅ merged | Per-control evidence fusion (questionnaire + scan); coverage matrix in portal; `governance:read`-gated list endpoint |
| PR 29 | HIPAA + SOC 2 Playbooks | ✅ merged | Dedicated HIPAA and SOC 2 governance execution playbooks; Privacy/Security Officer gates (HIPAA); Executive Sponsor + 8 document class gates (SOC 2); annual evidence freshness on all policy docs |
| PR 30 | Portal Engagement Selector | ✅ merged | localStorage-backed engagement persistence (`fg_portal_eid`); engagement selector hub on home page; auto-select single engagement; all 4 sub-pages fall back to stored ID when `?e=` param absent |
| PR 31 | Remediation Roadmap v1 | ✅ merged | Priority scoring (severity × scan evidence × NIST coverage); 3-phase execution roadmap (0–30/31–60/61–90 days); per-phase compliance delta preview; quick-wins matrix (impact vs effort); step-by-step runbooks |
| PR 32 | Remediation Closed Loop | ✅ merged | Client marks finding resolved with evidence notes; `FaFieldObservation` + `FaEvidenceLink` created; NIST questionnaire responses bumped `not_implemented`→`partial`; live roadmap refresh in portal |
| PR 33 | Risk Posture Dashboard | ✅ merged | Home page risk intelligence: NIST coverage bar (current vs projected), finding severity strip, NIST function heatmap (GOVERN/MAP/MEASURE/MANAGE), immediate actions callout; `reportlab` dependency added (unblocks PDF export); remediation steps rendered in findings page explainer |
| PR 34 | Console Auth Gate | ✅ merged | Auth0 OIDC login protection on console via next-auth v5; middleware protects all routes; SessionProvider + sign-out in sidebar |
| PR 35 | Portal Field Assessment Workspace | ✅ merged | `/engagement` list + `/engagement/[id]` tabbed detail (Overview, Scans, Documents, Observations, Evidence, History); 7 new portalApi methods; "Assessment" nav link |
| PR 36 | Workforce Intelligence | ✅ merged | Per-user AI query attribution; `tenant_users` + `ai_query_log` tables; subject-matter classification; risk scoring; workforce admin dashboard in console; AI workspace in portal |
| PR 37 | Risk History + Keywords + Alerting | ✅ merged | Daily risk score snapshots with Recharts trend chart; tenant-configurable keyword triggers (contains/exact/word_boundary/prefix/regex + case sensitivity); threshold-based alert rules with cooldown + fired-alerts audit log; keyword backtest/preview against historical queries; Keywords + Alerts tabs in workforce dashboard |
| PR 38 | Executive PDF Export | ✅ merged | Client-ready multi-page PDF: cover page, AI executive summary (advisory-labeled), confidence assessment, severity-sorted findings, remediation plan, framework coverage, evidence appendix, per-page footer with manifest hash; replaces raw-data stub |
| PR 39 | Production Deployment Fixes | ✅ merged | FA tables created on Postgres startup (`create_all checkfirst`); auth gate injects BFF header tenant into result to satisfy security gate; federated Auth0 sign-out via route handler (`/api/auth/logout`); middleware makes landing page public + uses pathname callbackUrl; Railway GitHub auto-deploy + CI fixes (pr-base-mainline skip on push, release-images FG_ENV, scorecard drift) |
| PR 40 | Three New Scan Connectors | ✅ merged | OAuth Inventory (MS Graph device-code: app registrations, service principals, OAuth grants); Endpoint Inventory (MS Graph device-code: Azure AD devices + Intune); Network Scan (pure Python: port scan + TLS inspection for 20 ports including AI model server ports); 3 bridges, 3 API endpoints, 3 console scan panels |
| PR 41 | DNS &amp; Email Security Connector | ✅ merged | DMARC policy/reporting, SPF record + all-mechanism, DKIM selector probing, MX presence, DNSSEC validation; dnspython-based pure-Python runner; bridge with NIST AI RMF GOVERN 6.2 mappings; console scan panel |
| PR 42 | Web Security Headers Connector | ✅ merged | HSTS (max-age, includeSubDomains), CSP (unsafe-inline/eval/wildcard), X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, plain-HTTP detection; httpx HEAD runner; bridge with NIST MANAGE 2.2/2.4 mappings; console scan panel |
| PR 45 | OAuth Risk Deep Scan Connector | ✅ merged | Illicit consent grant detection (user-consented critical/high-risk scopes — the consent phishing attack pattern); AI tool OAuth data access (ChatGPT, Claude, Copilot, Gemini + 15 patterns against Mail/Files/Calendar/Teams); over-privileged application permissions via MS Graph appRoleAssignedTo (3 API calls regardless of tenant size); unverified publisher + sensitive scope analysis; NIST MAP 1.1 + GOVERN 1.2/6.2 + MANAGE 2.4; Application.Read.All + Directory.Read.All scopes |
| PR 44 | SharePoint &amp; OneDrive Data Exposure Connector | ✅ merged | External sharing detection (anonymous "anyone with link", external domain users, org-wide links, no-expiry links); tenant domain resolution via /organization; 30-site / 3-drive / 100-item sampling with permission expansion; critical severity when ≥10 anonymous items; NIST MAP 1.1 + GOVERN 1.2/6.2; Sites.Read.All + Files.Read.All scopes; device-code console panel |
| PR 43 | Entra ID Governance Connector | ✅ merged | PIM permanent vs eligible role assignments; excess/stale Global Admin findings; Access Review coverage (privileged roles, recurring); Identity Protection risky users (P2-gated, graceful 403); Conditional Access posture (legacy auth block, MFA enforcement, report-only policies); 7 dedicated MS Graph scopes; NIST GOVERN 1.2/6.2 + MANAGE 2.2/2.4 mappings; device-code console panel |

---

## Phase 2 — Client Readiness
*Target: first client engagement by 2026-06-27.*  
*Every item below is a blocker or a strong-should for client delivery.*

### P0 — Blockers (cannot deliver without these)

| # | Item | Owner | PR | Status |
|---|------|-------|----|--------|
| 1 | **Portal authentication** — `middleware.ts` login gate; portal currently has no auth | — | PR 24 | ✅ done — HMAC-SHA256 session cookies, `/login` page, `PORTAL_PASSWORD` + `PORTAL_SESSION_SECRET` |
| 2 | **Scan trigger UI** — operator needs to initiate MS Graph device-code scan from console without running CLI | — | PR 25 | ✅ done — `ScanTriggerPanel` component; device-code flow with polling; status display in console engagement workspace |
| 3 | **NIST AI RMF questionnaire** — structured per-control manual evidence input; `FaFieldObservation` has no questionnaire schema | — | PR 26 | ✅ done — `fa_questionnaires` + `fa_questionnaire_responses` tables; 69 NIST AI RMF 1.0 controls; auto-seeded on init; per-control status + evidence; submit auto-links to findings |
| 4 | **Fix `VERIFY_BASE_URL`** — hardcoded as `"https://verify.fieldguide.io/report"` in `services/connectors/msgraph/report.py` | — | PR 23 | ✅ done — reads `FG_REPORT_VERIFY_URL`, defaults to `localhost:3001/verify` |
| 5 | **`.env.example`** — document `FG_MSAL_CLIENT_ID`, `FG_ACKNOWLEDGMENT_KEY`, `FG_CORE_TENANT_ID`, all required vars | — | PR 23 | ✅ done — all vars documented with descriptions and generation instructions |

### P1 — High-value for first client (ship before or during engagement)

| # | Item | Owner | PR | Status |
|---|------|-------|----|--------|
| 6 | **Executive summary in report** — currently no narrative opening section for client PDF | — | PR 27 | ✅ done — `executive_summary` section in report JSON; Claude narrative with risk posture + key concerns; graceful template fallback; rendered in console ReportViewer and portal inline view |
| 7 | **NIST control coverage matrix** — portal view: which controls have evidence, which are gaps | — | PR 28 | ✅ done — `GET /engagements/{id}/questionnaires` with evidence fusion (questionnaire + scan counts per control); `governance:read`-gated; portal Coverage page + nav link |
| 8 | **HIPAA + SOC 2 playbooks** — dedicated governance execution playbooks for healthcare and commercial compliance | — | PR 29 | ✅ done — `HIPAA_PLAYBOOK` (Privacy + Security Officer gates, 7 HIPAA document classes, BAA no-expiry) + `SOC2_PLAYBOOK` (Executive Sponsor gate, 8 Trust Service Criteria document classes, 6 observation domains, annual freshness) |
| 9 | **Portal engagement selector UI** — findings page currently requires engagement ID in URL, no picker | — | PR 30 | ✅ done — engagement selector hub on home page; localStorage persistence; all sub-pages fall back to stored ID |
| 10 | **Remediation roadmap in portal** — sequenced, owned remediation steps with priority ordering | — | PR 31 | ✅ done — priority scoring; 3-phase lanes; compliance delta preview; quick-wins matrix; per-finding step runbooks |
| 11 | **Azure AD app pre-registration guide** — required API scopes + admin consent walkthrough for MS Graph scan setup | — | PR 25 | ✅ done — `docs/operators/azure_ad_app_setup.md`: create app, 7 delegated permissions, public client flow, scan walkthrough, troubleshooting |

### P2 — Post-first-client (backlog)

| # | Item | Notes |
|---|------|-------|
| 12 | Redis-backed explanation cache | Replace in-memory LRU; needed for multi-worker |
| 13 | Explanation manifest persistence | Store `FindingExplanation` to DB alongside finding |
| 14 | `remediation_priority` scoring | ✅ done 2026-05-30 — 4-factor formula: severity base + exploitability class (by connector family) + confidence factor (integrates freshness degradation) + source bonus; `PHASE_IMMEDIATE_THRESHOLD=50`, `PHASE_SHORT_TERM_THRESHOLD=35`; effort mapping + step templates for all 9 connector families; 78 tests in `tests/test_remediation_scoring.py` |
| 15 | Evidence freshness degradation | ✅ done 2026-05-30 — `services/field_assessment/confidence.py`; decay table: 0–30d: ±0, 31–60d: −5, 61–90d: −15, 91+d: −30, floor 30; applied at read time in `_finding_to_response()`, report domain aggregation, and readiness low-confidence gate; 22 tests in `tests/test_confidence_degradation.py` |
| 16 | Cross-finding correlation | Surface related findings in explanation panel |
| 17 | Executive PDF export | PR 38 ✅ merged |
| 18 | Portal rate limiter → Redis | ✅ done 2026-05-31 — `apps/portal/lib/redis.ts`: lazy `ioredis` client via `PORTAL_REDIS_URL`; `checkRateLimit()` uses Redis INCR+EXPIRE (atomic fixed-window) with in-memory `_rlBuckets` fallback when Redis is unavailable |
| 19 | Dedicated CMMC/SOC2/ISO27001 playbooks | ✅ done 2026-06-02 — `CMMC_PLAYBOOK` (SSP + assessment scope + CUI marking + config mgmt + IR plan gates; IT Admin + Security Officer + System Owner + Compliance Owner roles; endpoint_inventory scan required); `ISO27001_PLAYBOOK` (ISMS scope + SoA + risk assessment/treatment + internal audit gates; CISO + Compliance Owner + System Owner roles; 5 observation domains); both registered in `_PLAYBOOKS`; cmmc/iso27001 removed from comprehensive fallback |
| 20 | Operator onboarding runbook | ✅ done 2026-05-30 — `docs/operators/onboarding_runbook.md` expanded: all 9 connectors, before-meeting (no-auth) + in-meeting split, P2-gated permission callout, full troubleshooting table |
| 21 | Connector cross-reference doc | ✅ done 2026-05-30 — `docs/CONNECTOR_CROSSREF.md`: client technology → connector → MS Graph scopes → data collected → findings matrix; MS 365 license tier impact table; no-auth connector section; data gaps disclosure table |
| 22 | PDF data collection disclosure appendix | ✅ done 2026-05-30 — New section in `export_pdf_bytes()`: per-connector data accessed table, retention/redaction/transmission statement, operator authorization note; populated from engagement scan results at export time |
| 23 | FA connector policy file | ✅ done 2026-05-30 — `contracts/connectors/policies/fg_field_assessment.json`: all 9 FA connectors with MS Graph scopes, `retention.days=90`, `redaction_mode=strict`; replaces Slack/Google Drive placeholder |
| 24 | First client readiness tracker | ✅ done 2026-05-30 — `CLIENT_READINESS.md` at repo root: 8-section checklist (infrastructure, monitoring, runbooks, policy, deliverables, governance, remediation tracking, dry-run script) with priority order and cross-references |
| 25 | Production deployment — Railway prod mode | ✅ done 2026-05-31 — `FG_ENV=prod` enforced; satisfied all 20+ startup validators (compliance module flags, ring state dir, mission envelope, CORS, encryption key, JWT secret, request limits); `state/.gitkeep` + `models/.gitkeep` force-added; `mission_envelope.json` committed at repo root |
| 26 | Health endpoint HEAD fix | ✅ done 2026-05-31 — `api/main.py`: removed duplicate `@app.get("/health")` at line 1097 that was shadowing the `api_route(methods=["GET","HEAD"])` registration; UptimeRobot HEAD checks now return 200 |
| 27 | Stripe webhook endpoint | ✅ done 2026-05-31 — Created webhook endpoint in Stripe Dashboard (`/stripe/webhook`); `STRIPE_WEBHOOK_SECRET` (`whsec_`) set in Railway; CLIENT_READINESS A4 confirmed |
| 28 | Azure AD app registration | ✅ done 2026-05-31 — Registered Azure AD app with 15 MS Graph delegated permissions + admin consent; public client flow enabled for device-code auth; `FG_MSAL_CLIENT_ID` set in Railway; CLIENT_READINESS A4 confirmed |
| 29 | Console user guide — all 9 scan panels | ✅ done 2026-05-31 — `docs/operators/console_user_guide.md`: all 9 scan panels documented (DNS/Email, Web Headers, Network, MS Graph Core, OAuth Inventory, OAuth Risk, Endpoint Inventory, Entra Governance, SharePoint + OneDrive); auth flow, pre-meeting vs in-meeting split, P1/P2 license caveats |
| 30 | Data Processing Agreement template | ✅ done 2026-05-31 — `contracts/dpa_template.md`: sub-processors table (Railway/Vercel/Anthropic/Auth0 with SOC 2 certs), 90-day retention, 72-hour breach notification, audit rights, dual signature blocks; F6 in CLIENT_READINESS |
| 31 | Secure credential delivery plan | ✅ done 2026-05-31 — `docs/operators/credential_delivery.md`: three delivery options (1Password Share, Bitwarden Send, verbal); reset instructions; split-channel rule; E11 in CLIENT_READINESS |
| 32 | H1–H18 dry run — end-to-end delivered | ✅ done 2026-06-01 — First full engagement dry run completed: engagement created → 7 scans → 7 docs → 7 evidence links → 4 interviews → 4 findings remediated → report generated + signed → QA approved → status `delivered`; 6 platform gaps found and fixed (see below) |
| 33 | BFF rate limiter fail-open | ✅ done 2026-06-01 — Rate limiter now skips (warns + passes) when Redis unavailable instead of returning 503 on every request; `BFF_REDIS_URL` + Upstash provisioned for production rate limiting |
| 34 | Finding remediation UI | ✅ done 2026-06-01 — `PATCH /engagements/{id}/findings/{id}/remediation` endpoint; `RemediationForm` in expanded finding cards; clears `remediation_hint` readiness gate without page reload |
| 35 | QA approve UI | ✅ done 2026-06-01 — `POST .../reports/{id}/qa-approve` wired to frontend; "QA Approve" button on finalized report rows in `ReportVersionHistory`; clears `qa_approved_report` gate |
| 36 | Guided panel live refresh | ✅ done 2026-06-01 — `GuidedExecutionPanel` re-fetches next actions immediately when `completed_gate_count` changes; interviews and remediation gates clear without page reload |
| 37 | Tab navigation from sidebar | ✅ done 2026-06-01 — `TAB_SECTIONS` map fixed (`report → reports`); sidebar "Fix this →" now scrolls main tabs into view and highlights the target tab trigger |
| 38 | Report signing key | ✅ done 2026-06-01 — `FG_REPORT_SIGNING_KEY` (Ed25519 seed) documented and set in Railway; report generation no longer throws `ReportSigningKeyError` |
| 39 | Engagement status simplification + auto-advance + client access code | ✅ done 2026-05-31 — Reduced engagement statuses from 10 → 6 (`in_progress`, `delivered`, `remediation`, `monitoring`, `closed`, `cancelled`); removed mechanical intermediates (`scheduled`, `pre_visit`, `evidence_collected`, `report_generation`); new engagements start as `in_progress`; QA approve auto-advances `in_progress` → `delivered` and generates an 8-char memorable client access code; code stored on engagement record and displayed prominently in console after delivery; legacy status values normalized on read; Postgres migration `0073` + SQLite auto-migration |
| 40 | FA Sprint 1 audit hardening | ✅ done 2026-06-01 — Auth check on transcribe endpoint (401 if no session); opaque tenant-scoped blob paths via SHA-256 hash; blob_warning field when upload fails; sector badge in interview guide; full gate IDs (not truncated 8-char); QA approval auto-refresh; `governance:qa_approve` scope for segregation of duties |
| 41 | FA Sprint 2 — data integrity + type expansion | ✅ done 2026-06-01 — Backend interview role validation against playbook (422 on invalid role); audio evidence Pydantic validator (_audio_url, _audio_hash, _audio_duration_sec); framework filtering by assessment type (CMMC controls only on CMMC engagements); silent domain drop logging; bulk observation import endpoint (200 rows, per-row errors); PCI DSS/DORA/FedRAMP/NIST 800-171 added to AssessmentType enum and creation form; client access code refresh on QA approve |
| 42 | FA Sprint 3 + competitive differentiators | ✅ done 2026-06-01 — Soft-delete observations (PATCH + DELETE endpoints, deleted_at column, cascade evidence links); observation change history via audit events; Postgres migration 0074; interview templates endpoint (cross-engagement, role+type filtered); clickable control gap matrix (DIFF-3: framework row → related observations inline); regulatory clause display per question (DIFF-1: HIPAA §164.308 etc. shown even without NIST ref); AI entity extraction from transcripts (DIFF-2: GPT-4o-mini post-Whisper pass → vendors, systems, risks, suggested domains) |
| 43 | FA forensic regression suite | ✅ done 2026-06-02 — Added 15 SQLite-isolated `test_fa_forensic_*` modules covering tenant write isolation, terminal evidence locks, evidence-link integrity, soft deletion, pagination, readiness, questionnaires, drift, audit append behavior, QA, remediation, connector lock behavior, report verification, and playbook registration |
| 52 | **Verification Bundle V1** | ✅ done 2026-06-03 — Deterministic SHA-256 hashed snapshot of all 9 engagement components (findings, evidence, interviews, governance decisions, risk acceptances, exceptions, audit trail, report). Tamper detection (broken finding refs, orphaned governance objects). Three new routes: `POST .../verification-bundle/generate` (`governance:write`), `GET .../verification-bundle`, `GET .../verification-bundle/manifest` (both `governance:read`). `services/verification_bundle/bundle_service.py` + `FaVerificationBundle` append-only table + migration `0086`. Console: "Generate Bundle" button with status, hashes, component counts, and tamper details in the reports tab (`VerificationBundlePanel`). Portal: read-only bundle status card on engagement Overview tab. 32 tests (V1–V32). All CI gates pass. |
| PR 4 | **Third-Party AI Governance Workflow Engine** | ✅ done 2026-06-04 — 13th scan type. Passive connector consuming PR 3 risk records to generate governed organizational assets. `FaAiVendorGovernanceRecord` (~70 columns, one per vendor/engagement/tenant) with 8-state governance workflow (discovered → needs_owner → needs_review → approved/restricted/rejected/exception_granted → retired). Append-only `FaAiVendorGovernanceDecision` ledger with Postgres-level UPDATE/DELETE triggers. `governance_readiness` (complete/partial/minimal/unknown) deterministic server-side — never patchable. 16 finding types mapped to NIST AI RMF controls (GOVERN 1.1/1.2/1.3/1.4/6.1/6.2, MAP 1.1, MANAGE 2.2/2.4). `target_type` extensible to vendor/ai_tool/ai_agent/autonomous_system/agent_swarm/decision_engine/agi_provider — AGI governance without schema migration. 5 routes: run, list, PATCH, transition, decisions. `AiVendorGovernanceUpdateRequest` with `extra="forbid"` prevents immutable field injection. Executive summary: 14 metrics (state/readiness/criticality distributions, needs_owner/review/overdue/expiring counts, no_dpa/baa/contract/security counts). `exception_granted` preserved across re-scans. Console `AiGovernancePanel` (TanStack Query, transition modal, decision ledger). Portal `AiGovernancePortalTab` (read-only). Verification bundle: `ai_vendor_governance` + `ai_vendor_governance_decisions` components. Migration 0092. 67 tests (W/G/S/L/R/D series). All CI gates pass. |
| PR 3 | **External AI Risk Register** | ✅ done 2026-06-03 — 12th scan type. Passive connector consuming PR 1 (AI Tool Discovery) + PR 2 (AI Data Access Mapping) to generate risk-scored records. 8 risk categories: tenant_wide_permissions, sensitive_data_access, unverified_publisher, overprivileged_oauth, shadow_ai, unknown_owner, no_dpa_baa_vendor_review, no_approval_record. Addendum: governance_state (ungoverned/partially_governed/governed/exception_granted), regulatory_flags (EU_AI_ACT, NIST_AI_RMF, ISO_42001, GDPR, HIPAA, PCI_DSS, SOX, GLBA, FFIEC, State_Privacy_Law), remediation_status, risk_age_days, vendor governance fields. Exception_granted preserved on re-scan. Build_summary with governance/vendor/regulatory/remediation distributions + stale_risks. Migration 0090+0091. 124 tests. All CI gates pass. |
| PR 2 | **AI Data Access & Flow Mapping** | ✅ done 2026-06-03 — Deterministic governance intelligence layer extending PR 1. `AiDataAccessMapper` maps each AI tool's permissions → MS Resource → Business Data Category → Sensitivity (unknown/low/moderate/high/critical). Addendum fields: `data_owner`/`owner_type` (IT, Operations, Unknown — inferred from data category), `review_status` (always "unreviewed" on creation), `exposure_scope` (tenant/group/user/unknown — derived from admin_consent + assigned_users), `governance_readiness` (governed/partially_governed/ungoverned/unknown — deterministic from verified_publisher + review_status). 5 finding types: critical_data_access, tenant_wide_sensitive_access, sensitive_data_access, multi_category_sensitive_access, unverified_sensitive_access. Executive summary metrics in scan summary: governance_readiness_distribution, scope_distribution, owner_distribution. Graph-ready node IDs on every mapping: data_access_node_ids, resource_node_ids, owner_node_id, scope_node_id, governance_node_id. No new MS Graph calls — all fields derived from PR 1 FaScanResult. H12 durable job + H13 full audit instrumentation (no exception needed — direct route calls). Report section ai_data_access_mapping. Console AiDataAccessMappingPanel (synchronous, no device-code). Portal AiDataAccessDetails inline in Scans tab. Migration 0089. 50+ tests. |
| PR 1 | **AI Tool Discovery Scan** | ✅ done 2026-06-03 — 10th scan type: evidence-backed AI governance discovery from Microsoft Graph. Data-driven vendor registry (12 vendors: Copilot, ChatGPT, Claude, Gemini, Perplexity, Cursor, GitHub Copilot, Notion AI, Grammarly, Canva AI, Salesforce AI, ServiceNow AI) with exact/fuzzy/publisher/domain/app-id matching. MS Graph runner (app registrations, service principals, OAuth grants, app roles, sign-in logs, audit logs; graceful 403/404). Bridge imports findings to FA evidence pipeline with NIST MAP 1.1 + GOVERN 6.2 + MANAGE 2.4 mappings. H12 durable scan job + H13-atomic audit events throughout. Report section `ai_tool_discovery` with tool inventory (tool_name, vendor, publisher, verified_publisher, permissions_summary, admin_consent, last_seen, risk_indicators, evidence_refs, confidence, status). Console `AiToolDiscoveryScanPanel` (device-code flow, native scan catalog). Portal `AiToolDetails` inline in Scans tab. `/portal` mapped to control plane in registry. Migration `0088`. 10 tests covering vendor detection, permissions, admin consent, verified publisher, risk indicators, 403 handling, deterministic ordering, tenant isolation, engagement isolation, report section. All CI gates pass. |
| PR 17.7D | **CGIN Transparency Authority** | ✅ done 2026-06-29 — Append-only Merkle-backed transparency ledger for CGIN governance operations. Created `services/cgin/transparency/` package: `entry.py` (`TransparencyEntry` frozen dataclass, `_compute_entry_id`, `TRANSPARENCY_VERSION`, `TRANSPARENCY_SCHEMA_VERSION`); `merkle.py` (`MerkleTree` with SHA-256 domain-separated hashing — leaf: `SHA256(0x00 || data)`, interior: `SHA256(0x01 || L || R)`; `MembershipProof` frozen dataclass with `to_dict`/`from_dict`; `EMPTY_LEAF`); `root.py` (`TransparencyRoot` frozen dataclass); `store.py` (`TransparencyStore` `@runtime_checkable Protocol`; `MemoryTransparencyStore` — append-only, duplicate raises `ValueError`); `ledger.py` (`TransparencyLedger` — `append`, `build_root`, `membership_proof`, `verify_entry`, `statistics`); `verify.py` (`TransparencyVerificationResult` dataclass; `verify_entry_in_store`); `statistics.py` (`IntegrityStatistics` frozen dataclass; `compute_statistics`); `__init__.py` (`ACTIVE_TRANSPARENCY_LEDGER` singleton). Created `api/cgin_transparency.py`: 7 routes (`GET /cgin/transparency/root/latest`, `GET /cgin/transparency/root/{root_id}`, `GET /cgin/transparency/entries/{entry_id}`, `GET /cgin/transparency/proof/{entry_id}`, `POST /cgin/transparency/verify`, `GET /cgin/transparency/statistics`, `GET /cgin/transparency/health`) — all `require_scopes("governance:read")`, tenant-isolated, Pydantic response models, `tags=["cgin-transparency"]`. Registered router in `api/main.py`. Added `/cgin/transparency` prefix to `control` plane in `services/plane_registry/registry.py`. Created CI gate `tools/ci/check_cgin_transparency.py` (AST + runtime determinism + append-only invariant checks). 225+ tests in `tests/test_cgin_transparency.py` across 12 classes. No new dependencies. No DB schema changes. No migrations. |
| PR 17.7C | **CGIN Enterprise Key Management Authority** | ✅ done 2026-06-30 — Provider-based key management architecture on top of 17.7B. Created `services/cgin/key_management/` package: `provider.py` (moved `SigningAlgorithm`/`ACTIVE_SIGNING_ALGORITHM` here; defines `KeyProvider` as `@runtime_checkable Protocol`, `ProviderHealth`, `ProviderCapabilityManifest`, `ProviderMetadata`, `AuditEvent`, `CryptoPolicy`); `registry.py` (`ProviderRegistry` with duplicate-name validation + `ACTIVE_PROVIDER_REGISTRY` singleton seeded with ephemeral `MemoryKeyProvider`); `providers/memory.py` (`MemoryKeyProvider` — in-process Ed25519 provider, exact 17.7B behaviour preserved); 6 enterprise stub providers (AWS KMS, Azure Key Vault, Google KMS, HashiCorp Vault, PKCS#11, HSM Generic) — all `health()=NOT_IMPLEMENTED`, `sign`/`verify` raise `NotImplementedError`. Refactored `trust.py`: removed `Ed25519PrivateKey/PublicKey` imports; `sign_payload`/`verify_payload` delegate to `as_provider(key).sign/verify`; `SigningAlgorithm`/`ACTIVE_SIGNING_ALGORITHM` re-exported for backward compat — all 173 existing tests pass unchanged. `trust_manifest.py`: renamed `private_key→signing_key`, `public_key→verification_key`. `api/cgin_trust.py`: removed `Ed25519PrivateKey` import; `get_manifest` uses `ACTIVE_PROVIDER_REGISTRY.active()`; 3 new routes (`GET /cgin/trust/providers`, `GET /cgin/trust/providers/active`, `GET /cgin/trust/providers/{provider_name}`). Created CI gate `tools/ci/check_cgin_key_management.py`. 256 tests in `tests/test_cgin_key_management.py` across 14 classes. No new dependencies. No DB schema changes. No migrations. |
| PR 17.7B | **CGIN Trust & Integrity Authority** | ✅ done 2026-06-29 — Every CGIN snapshot is now cryptographically verifiable. Created `services/cgin/trust.py`: `SigningAlgorithm(str, Enum)` with `ED25519_V1 = "ed25519-v1"` and commented future slots (ED448, DILITHIUM, SPHINCS); `ACTIVE_SIGNING_ALGORITHM = SigningAlgorithm.ED25519_V1`; `canonicalize_snapshot` (recursive key sort, UTF-8 JSON, `separators=(',', ':')`); `generate_digest` (SHA-256, 64-char hex, no truncation); `sign_payload` / `verify_payload` (Ed25519 via `cryptography` 46.0.7, base64url no-padding); `VerificationResult` dataclass; `verify_snapshot` (tamper-detects changed values, added/removed fields, altered metadata, altered fingerprints, partial corruption — never raises); `build_trust_metadata` (embeds `schema_version`, `digest`, `signature`, `signing_algorithm`, `fingerprint_algorithm`, `created_at`, `authority_version`, `trust_version`, `canonicalization_version`, optional `previous_snapshot_digest`). Created `services/cgin/trust_manifest.py`: `TrustManifest` dataclass; `generate_trust_manifest`; `verify_trust_manifest` (never raises). Created `api/cgin_trust.py`: 4 routes (`GET /cgin/trust/algorithms`, `POST /cgin/trust/verify`, `GET /cgin/trust/verify`, `GET /cgin/trust/manifest/{snapshot_id}`) — all `require_scopes("governance:read")`, tenant-isolated, Pydantic response models, `tags=["cgin-trust"]`. Registered router in `api/main.py`. Created CI gate `tools/ci/check_cgin_trust.py` (AST + lightweight import; mirrors `check_cgin_privacy.py`). 168 tests in `tests/test_cgin_trust.py` across 10 classes. No new dependencies (uses `cryptography` already in `requirements-shared.txt`). No DB schema changes. No migrations. Algorithm rotation = one constant change (`ACTIVE_SIGNING_ALGORITHM`). |
| PR 17.7A | **CGIN Privacy Hardening Authority** | ✅ done 2026-06-28 — Replaced raw `tenant_id` in CGIN snapshot payloads for 6 legacy authorities with deterministic `tenant_fingerprint` (`sha256("cgin:v1:{tenant_id}")[:32]`). Created canonical shared helper `services/cgin/privacy.py` (`fingerprint_tenant`, `assert_snapshot_safe`). Updated schemas: `CGINEffectivenessSnapshot`, `CGINSnapshotBundle` (+ nested `EvidenceStatusSnapshot`, `VerificationSnapshot`, `CoverageSnapshot`, `HealthSnapshot`), `FreshnessCGINSnapshot`, `FreshnessCGINTrendSnapshot`, `CGINRemediationSnapshot`, `WorkflowCginSnapshot` — all now use `tenant_fingerprint: str`. Updated 6 engines to import from `services.cgin.privacy`. Updated `authority_manifest.yaml`: 6 authorities set `cgin_anonymized: true`, `privacy_version: "1.0"`, `fingerprint_algorithm: "sha256-cgin-v1"`. Upgraded CI gate (`tools/ci/check_authority_integration.py` Check 7) from warning to FAIL. 140 new tests in `tests/test_cgin_privacy.py`. No new routes, no DB schema changes, no migrations, no auth changes. |

---

## Phase 3 — Enterprise Ready
*Detailed delivery plan in `ENTERPRISE_PLAN.md` (authoritative for all Phase 3+ work).*

**Gate:** Controlled pilot → Enterprise production → Regulated enterprise.

Sequencing summary — see ENTERPRISE_PLAN.md for full spec:

| Gate | Key work | Estimate |
|------|----------|----------|
| **Phase 0A — Revenue-Safe** | ✅ C5 audio proxy SSRF — artifact-registry refactor (PR fix 43); ✅ C6 scanner containment hardening (PR fix 44): SafeTargetValidationService, 12-layer validation, DNS-rebinding protection, redirect revalidation, durable scan jobs, append-only audit trail, 114 security tests; ✅ C7 portal grant model (PR fix 45): Argon2id-hashed grants, server-side sessions, engagement-binding, append-only audit trail, middleware rewritten, 46 security tests, 15 security layers; ✅ H13 audit atomicity (PR fix 46): AuditAtomicityService, report split-commit fixed, 5 unaudited paths fixed, transaction_id/entity correlation columns, migration 0082, 33 tests; ✅ H13.5 audit coverage enforcement (PR fix 47): AuditCoverageValidator CI gate, AST route discovery, exceptions registry, `make audit-coverage-check` in fg-fast, 25 security tests, 100% coverage; ✅ H15 evidence lifecycle locks (PR fix 49): EvidenceLifecycleService, 3-state machine (collected→locked→legal_hold), direct collected→legal_hold path, bulk QA-approval lock, DB triggers on 4 evidence tables + governance_reports, chain-of-custody FaEvidenceLifecycleEvent + FaLegalHold, migration 0083, 31 security tests | Weeks 1–2 |
| **Phase 1 — Trusted Pilot** | ✅ H12 durable scan jobs (PR fix 50): DurableJobService, all 9 scan routes create FaScanJob, lease model (hostname:pid, 300s TTL), exponential backoff retry, dead-letter routing, orphan recovery, DB fallback in status route, `GET /scan-jobs` + `GET /scan-jobs/{job_id}` routes, migration 0084, 21 tests; ✅ H14 governance decision ledger (PR fix 51): GovernanceDecisionService (single write authority), FaGovernanceDecision + FaRiskAcceptance + FaGovernanceException append-only tables, DB triggers (UPDATE/DELETE forbidden), H13-atomic decision+audit event, full actor attribution (actor_id, actor_name, actor_email, actor_role), approval chain (creator/reviewer/approver), evidence_snapshot_hash provenance, qa_approve + finding_closed wired to decision ledger, 8 new governance routes, migration 0085, 35 tests; ✅ **H14 Enterprise RBAC + Human Actor Attribution** (2026-06-04): Auth0 JWT identity layer (RS256 JWKS validation); provider-agnostic ActorContext + IdentityProvider protocol (Auth0/Entra/ApiKey/stub); permission-based authorization (not role-based) with require_permission() FastAPI dependency; 6-role enterprise model (viewer/assessor/qa_reviewer/compliance_reviewer/tenant_admin/platform_admin) with SoD invariants (tenant_admin ≠ compliance authority, assessor ≠ self-approver); 5 critical governance routes hardened (qa-approve, risk-accept, exception-grant, bundle-generate, governance-decision); actor attribution spoofing eliminated (actor_name/email/role stripped from request bodies, sourced exclusively from JWT ActorContext); fa_governance_events append-only typed event ledger with benchmark intelligence fields (event_version, schema_version, decision_reason first-class, review_duration_seconds, industry_sector, delegated_by delegation fields); actor_subject (Auth0 sub) added to fa_governance_decisions; migration 0098; 75 tests (P/D/V/A/Q/C/T/X/S/J/G series); H14_RBAC_GAP_REPORT.md; docs/operators/auth0_roles.md; H11 drift RLS, PI20 FA/Gov outbox boundary, DB hardening, portal session identity | Weeks 3–6 |
| **Phase 2 — Enterprise Production** | Governed document pipeline, retention + legal hold, scheduler execution, observability dashboards, assessor assignment workflows, client account identity, OpenAPI → TypeScript codegen | Weeks 7–14 |
| **Phase 3 — Moat Layer** | Longitudinal evidence graph (across reassessments), regulator-ready verification bundles (signed, replayable), reassessment intelligence (drift, regression, remediation velocity), consent-based sector benchmarks | Months 3–6 |
| **Phase 4 — Regulated Enterprise** | SOC 2 Type II readiness, FedRAMP preparation, HITRUST, air-gap mode, penetration testing | Months 4–12 |

---

### Active Identity Foundation PRs

| PR | Item | Status | Deliverable |
|---|---|---|---|
| PR 1 | Tenant Identity Schema + Identity Policy Foundation | ✅ merged (#409) | Explicit tenant identity policy, safe invitation lifecycle, membership subject binding, append-only identity audit events, and demo migration foundation |
| PR 2 | Provider-Neutral Admin Gateway Identity Enforcement | ✅ merged (#410) | Invitation start-auth, callback validation, identity binding, tenant-governed session issuance, replay protection, audit chain, Console BFF tenant_id override removed |
| PR 3 | Auth0 Adapter For Provider-Neutral Admin Gateway | ✅ merged | Auth0 org/connection provisioning, org-aware SSO login, managed signup/login, JWKS-backed token verification, claim normalization, fail-closed provisioning, 13 Auth0 audit event types; P1 fixes: code exchange, Auth0 wiring, server-side connection_id, org ownership guard |
| PR 4 | Console Identity Governance Control Plane | ✅ merged | 13 operator-level identity routes (config CRUD, readiness+evidence, invitations, audit-summary, governance scorecard, drift detection, timeline, readiness-history, risk engine); identity:read/write scopes; Console BFF proxy; `IdentityGovernancePanel` with 8 sub-tabs; full identity_type support (human/service/agent/system/workload); 27 backend tests |
| PR 5 | Legacy Removal + Governed Identity Cutover | ✅ merged | 410 tombstone for accept-invite; invite_user fail-closed (requires identity config); invitation_id/invitation_url replace invite_token/invite_url_hint; list_users returns identity_binding_status; migration 0101 (COALESCE, legacy_invite_disabled_at/reason); 4 new drift types (LEGACY_INVITE_PRESENT, UNBOUND_ACTIVE_USER, UNKNOWN_IDENTITY_TYPE); 7 new audit events; risk engine extended; BFF 410 gate; Console TypeScript updated; 21 new tests |
| PR 6 | Identity Governance Depth: Score, Drift, Risk, Timeline, Type, Provenance | ✅ merged | **M1** Governance score: 14 explicit dimensions (100 pts) — bound_identity_percent, no_unbound_active, verified_identity_percent, no_expired/legacy/revoked, identity_type_mix, domains_verified, multi_provider, audit_chain_intact; **M2** Drift auto-remediation: recommended_action + remediation_risk on every drift item; **M3** Timeline labels: _TIMELINE_LABELS mapping for all 24 event types (Invited/Bound/Activated/Role Assigned etc.); **M4** Risk scoring: 5 new factors — multiple_failed_bindings, provider_mismatch, domain_mismatch, repeated_invite_attempts, dormant_accounts; **M5** Identity Type Governance endpoint: GET /identity-types — distribution + risk_by_type (bind_rate, risk_band) per human/service/agent/system/workload; **M6** Session Provenance endpoint: GET /provenance?email= — full invitation chain + audit chain with labels; **M7** Dashboard: IdentityTypesPanel + ProvenancePanel tabs, DriftPanel shows remediation arrows, TimelinePanel shows human labels |
| PR 7 | Identity Governance Control Plane: Approval, Policy, Snapshots, Recommendations | ✅ merged | 8 new operator-level routes: policy-violations (rules engine: non-human-admin, unauthorized-domain), approval request/approve/reject/queue (server-side state machine with 409 guards — cannot approve twice, cannot approve after reject, cannot approve revoked/expired), governance-snapshots (append-only score history, POST+GET), recommendations engine (per-dimension action + expected_score_gain + projected_percent_if_all_applied); migration 0102 (approval_required/state/reason on tenant_invitations, tenant_identity_governance_snapshots table); 4 new audit event types; 22 wrong-tenant + state machine + immutability + evidence tests; route inventory updated |
| PR 8 | Identity Governance Intelligence: Trend, Forecast, SLA, Benchmark, Findings | ✅ merged | 5 intelligence routes on top of snapshot foundation: **Trend** (grade A→B delta, degraded/improved/stable-failing per dimension, narrative sentences); **Forecast** (pure-Python least-squares regression over 90-day snapshots, 30-day projection clamped [0,100], at-risk dimension annotation); **SLA** (server-side `_SLA_DAYS` thresholds per severity type, on_track/at_risk/breached per open item, summary counts); **Benchmark** (cross-tenant anonymized p25/median/p75/p90, own percentile rank — no tenant IDs exposed); **Findings** (unified finding object aggregating policy violations + risk dimensions + unbound-admin drift, deduplicated by finding_id Set, sorted critical-first, evidence dict for assessment bridge); 5 new TS interfaces + panels (TrendPanel, ForecastPanel, SlaPanel, BenchmarkPanel, FindingsPanel); tabs extended 14 → 19; 22 new tests (88 total); route inventory updated |
| PR 9 | Identity Governance Actions Ledger | ✅ merged | Append-only ledger for governance decisions on recommendations. Server-enforced state machine: first→{accepted,rejected,deferred,implemented}; accepted→{implemented,deferred}; deferred→{accepted,rejected}; rejected/implemented terminal (409). 3 routes: POST record action (actor_id/email/role, reason, outcome, deferred_until, snapshot_id), GET ledger (filterable/paginated), GET summary (latest-per-dimension via NOT EXISTS subquery). `audit_chain_intact` added to _DIMENSION_RECOMMENDATIONS. `tenant.identity_governance.action_recorded` audit event. ActionsLedgerPanel (state count cards, dimension table, inline record form, full ledger toggle); tabs 19→20. migration 0103. 14 new tests (102 total). | 5 intelligence routes on top of snapshot foundation: **Trend** (grade A→B delta, degraded/improved/stable-failing per dimension, narrative sentences); **Forecast** (pure-Python least-squares regression over 90-day snapshots, 30-day projection clamped [0,100], at-risk dimension annotation); **SLA** (server-side `_SLA_DAYS` thresholds per severity type, on_track/at_risk/breached per open item, summary counts); **Benchmark** (cross-tenant anonymized p25/median/p75/p90, own percentile rank — no tenant IDs exposed); **Findings** (unified finding object aggregating policy violations + risk dimensions + unbound-admin drift, deduplicated by finding_id Set, sorted critical-first, evidence dict for assessment bridge); 5 new TS interfaces + panels (TrendPanel, ForecastPanel, SlaPanel, BenchmarkPanel, FindingsPanel); tabs extended 14 → 19; 22 new tests (88 total); route inventory updated |
| PR 10 (P1) | Enterprise Identity Consolidation — Auth0 OIDC + Membership Enforcement | 🔄 open (#446) | **IdentityResolver** new shared service maps (provider, issuer, subject) → `IdentityPrincipal` via raw SQL against `tenant_users`; `resolve_or_deny()` enforces MEMBERSHIP_NOT_FOUND→MEMBERSHIP_INACTIVE typed exceptions. **Core API membership enforcement**: `_bind_membership()` in `auth_dispatch.py` runs post-JWT for `oidc_auth0` actors; MEMBERSHIP_INACTIVE → 403; `ActorContext.membership_id` field added for downstream propagation. **Admin gateway OIDC hardening**: replaced insecure `parse_id_token_claims()` (base64-decode) with `verify_id_token()` backed by JWKS RS256/ES256; `create_session_from_tokens()` calls `IdentityResolver`; `tenant_governed=True` only after active bound membership confirmed. **Console enforcement**: `require_governed_session()` FastAPI dependency — HTTP 403 SESSION_NOT_GOVERNED for ungoverned sessions. **Portal named-user flow (P1)**: `POST /portal/identity/login` core API endpoint; portal TypeScript OIDC routes (PKCE via Web Crypto API, no new npm deps); `fg_oidc_state` cookie; Auth0 code exchange; session cookie issuance. **Audit**: 3 new identity session denial event types in `IDENTITY_AUDIT_EVENTS`. 26 security tests (IR/AD/CS/PL/DE/AU series). |
| PR 11 (P1.1) | Membership Versioning + Immediate Session Revocation | 🔄 open (feat/p1-1-membership-versioning) | **Mechanism**: `membership_version BIGINT NOT NULL DEFAULT 1` on `tenant_users` (migration 0117). Incremented via `MembershipVersionService.bump_version()` on active/role/binding/trust/risk changes. Sessions embed version at issuance; mismatch = immediate revocation (no blocklist, no sweep jobs). **Admin gateway**: `Session.membership_version` field; `oidc.py` embeds `principal.membership_version`; `require_governed_session()` validates live DB version via async query — mismatch → HTTP 401 SESSION_REVOKED. **Portal named-user engagement access (fixes proxy/core gap)**: `portal_scope.py` new named-user path validates `X-FG-Membership-ID` + `X-FG-Membership-Version` headers against `tenant_users` — SESSION_REVOKED_VERSION_MISMATCH on stale version, MEMBERSHIP_INACTIVE on deactivated user. **Portal session**: `SessionUser.membershipVersion` in TypeScript; embedded in signed `fg_portal_session` cookie from OIDC callback; sent as `X-FG-Membership-Version` by portal proxy for OIDC users. **Core API**: `IdentityPrincipal.membership_version` field; `/portal/identity/login` returns `membership_version`. **Audit**: 7 new event types (version_changed, deactivated, reactivated, role_changed, binding_changed, risk_state_changed, session_denied_version_mismatch). 15 security tests (MV-1 through MV-15). |
| PR PR_NUMBER (P1.2) | Tenant Policy Bundles + Capability Framework | 🔄 open (feat/p1-2-capability-framework) | **Commercial authorization architecture**: 6 new DB tables (migration 0118): `tenant_subscriptions`, `policy_bundles`, `capabilities`, `policy_bundle_capabilities`, `tenant_capability_assignments`, `tenant_bundle_assignments`. **CAPABILITY_REGISTRY** extended with 27 new keys: `portal.*`, `ai.*`, `api.access`, `identity.*`, `reports.*`, `tenant.multi_region`, `msp.*`, `government.*`. **`services/capability_bundles/`**: `resolver.py` (`resolve_tenant_capabilities()` with TTL cache, `FG_CAPABILITY_CACHE_TTL_SECONDS`, `invalidate_cache()`), `seeder.py` (`seed_bundle_catalog()` with 7 canonical bundles: portal_only, portal_remediation, portal_ai, portal_ai_rag, enterprise, government, msp). **`check_capability()` resolution order**: registry miss → explicit grant → bundle/capability assignment (new) → tier fallback (backward compatible). **Bundle management admin routes**: `GET /admin/bundles`, `GET/POST /admin/tenants/{tid}/bundles`, `DELETE /admin/tenants/{tid}/bundles/{key}`, `POST /admin/tenants/{tid}/subscriptions`. **Audit events**: capability_assigned, capability_removed, bundle_assigned, bundle_removed, subscription_changed. 16 security tests (CAP-1 through CAP-15 + CAP-16). |
| PR PR_NUMBER (P1.4) | Subscription Assignment Engine | 🔄 open (feat/p1-4-subscription-assignment-engine) | **Commercial authority layer**: `SubscriptionContract → SubscriptionItem → TenantBundleAssignment` pipeline. 3 new ORM models: `subscription_contracts`, `subscription_items` (with MSP `parent_item_id` extension point and `meter_code` metering readiness), `subscription_event_ledger` (append-only, SHA-256 hash chain, ORM-level immutability guard). **`SubscriptionEngine` service** (`services/subscriptions/`): contract CRUD, item lifecycle (active/suspended/canceled/expired), automatic `TenantBundleAssignment` sync on status transitions (create → assign, suspend/cancel → expire), ledger appending. **No capability logic duplicated** — capability resolution continues through existing `check_capability` / `resolve_tenant_capabilities` (P1.2). **10 admin routes** (`api/subscriptions.py`): create/get/patch contracts, list contracts per tenant, create/get/patch items, list items per tenant, item ledger. **`GET /subscriptions/explain-capability`**: traces full resolution chain (registry → explicit grant → bundle/subscription → tier) with dependency checks per transitive dep. **5 Prometheus metrics** (no tenant_id): `subscription_contracts_created_total`, `subscription_items_created_total`, `subscription_items_status_changes_total`, `subscription_event_ledger_entries_total`, `subscription_explain_requests_total`. 20 tests (SUB-1 through SUB-20). |
| PR PR_NUMBER (P1.3) | Capability Enforcement Engine | 🔄 open (feat/p1-3-capability-enforcement) | **Fail-closed enforcement**: `require_capability(cap)` FastAPI dependency raises HTTP 403 CAPABILITY_DENIED on denial when `FG_ENTITLEMENT_ENFORCEMENT=true` (default). Enforcement flag read at call time so integration tests can override via env var. **Dependency graph**: `services/capability_enforcement/graph.py` — 10-edge prerequisite DAG (`DEPENDENCY_GRAPH`); BFS transitive resolver (`get_required_capabilities()`); DFS cycle detector (`detect_cycles()`); startup validator (`validate_graph()` — raises `ValueError` on cycles or dangling refs; logged + re-raised in prod). **Startup**: `validate_graph()` called in `api/main.py` lifespan after bundle seeder. **AI route gating**: `GET /ui/ai`, `GET /ui/ai/experience`, `GET /ui/ai/usage` → `require_capability("ai.workspace")`; `POST /ui/ai/chat` → `require_capability("ai.chat")`. **Audit events**: 5 new `EventType` values — `CAPABILITY_CHECK`, `CAPABILITY_GRANTED`, `CAPABILITY_DENIED`, `CAPABILITY_DEPENDENCY_FAILURE`, `CAPABILITY_UNKNOWN`. **Prometheus metrics**: 6 new counters — `frostgate_capability_{checks,grants,denials,dependency_failures,cache_hits,cache_misses}_total`; no `tenant_id` label. **Schema additions** (pre-merge on 0118): `billing_category`, `launch_stage`, `visibility` columns on `capabilities`; new `capability_dependencies` table (10 rows seeded); new `capability_meter_mappings` table (9 rows seeded). 30 security tests (CAPE-1 through CAPE-16). |
| PR PR_NUMBER (P1.5) | Billing Integration Layer | 🔄 open (feat/p1-5-billing-integration-layer) | **Provider-agnostic billing bridge**: connects P1.4 SubscriptionEngine to external billing providers. Billing observes and monetizes commercial state but NEVER controls entitlement access. **5 new ORM models**: `billing_accounts`, `billing_subscription_links`, `usage_meters`, `usage_events` (append-only, idempotency UniqueConstraint), `billing_event_ledger` (append-only, SHA-256 hash chain). **`services/billing/`** new package: `BillingProvider` ABC + `NullBillingProvider`; `StripeProvider` (lazy import, `ProviderNotConfiguredError`); `BillingEngine` (accounts, links, meters, usage, explain); `record_usage_event` (idempotency, ledger, optional provider report); `BillingReconciler` (pending retry, failed link retry). **15 new routes** (`api/billing_v2.py`): POST/GET/PATCH accounts; POST/GET/list/sync subscription-links; POST/GET/PATCH meters; POST usage events (`require_bound_tenant`); GET usage list (admin:read); POST `/billing/webhooks/stripe` (public, sig-verified, idempotency, no entitlement side-effects); GET billing explain. **8 Prometheus metrics** (no tenant_id label). **36 tests** (BILL-1 through BILL-35). |
| PR-01a.1 | Identity Runtime Integration and Authorization Alignment | 🔄 open (feat/pr-01a1-identity-runtime-integration) | **Wire FIAP + Identity Governance into live request path** — all behind feature flags in `api/config/identity_runtime.py`, defaulting to disabled. New: `apply_governance_checks()` invoked at the tail of `get_actor_context()`; risk engine + SessionEvaluator behind flags; non-ALLOW decisions raise machine-readable HTTPException (`IdentityErrorCode` enum in `api/identity_governance/error_codes.py`); fail-closed on internal error (500 `GOVERNANCE_UNAVAILABLE`); timeline emission best-effort. `GovernanceServices` singleton container. Repository abstraction (`api/identity_governance/repositories/`): Protocol base + in-memory (default) + SQLAlchemy (opt-in via `FG_IDENTITY_PERSISTENCE_ENABLED`) targeting 0148 tables. AuthorizationContext→ActorContext adapter. Prometheus counters (no PII/tenant labels). 93 new tests, all identity_governance suite green (228 tests). Docs: RUNTIME_ENFORCEMENT.md, AUTHORIZATION_MAPPING.md, GOVERNANCE.md + ARCHITECTURE.md updated. Baseline regressions unchanged. |
| PR-01a | FIAP Hardening + Identity Governance Foundation | 🔄 open (feat/pr-01a-identity-governance) | **Phase 0 — Fix PR-01 mypy errors** (17 errors → 0 in `api/identity_authority/`): `ActorContext` TYPE_CHECKING import in models; Redis client typed `Any` with rationale; `SecurityAuditor.log_event` call rewritten to construct proper `AuditEvent`; `LegacySessionMigrator._admin_secret: bytes` explicit; `algorithms=sorted(_SUPPORTED_ALGORITHMS)` (list) at Entra decode; `candidates: list[IdentityProviderProtocol]` in registry; `MachineIdentityRecord` test typing. **Phase 1 — new `api/identity_governance/` package**: 12 modules — `IdentityLifecycleManager` (8-state deterministic FSM), `DeviceTrustRegistry` (6 states, deterministic risk), `SessionEvaluator` (6-check pipeline: identity_state → session_expiry → session_revocation → device_state → mfa → risk), `ConditionalAccessPolicyEngine` (priority ASC, DENY overrides ALLOW at same priority, 7 condition kinds), `IdentityTimeline` (in-memory hash-chained, tenant-scoped), `IdentityGraphExporter` (deterministic snapshot, SHA-256 fingerprint excludes timestamps), `DelegatedAdminAuthority` (`ADMIN_LEVEL_ORDER` PLATFORM=0..ENGAGEMENT=6, `assert_no_escalation`), `BreakGlassAuthority` (reason-required, `MAX_BREAK_GLASS_DURATION_SECONDS=14400`, approver≠requester, self-expiring, timeline-emitting), `IdentityRiskEngine` (`EVALUATOR_VERSION="1.0.0"`, deterministic, factors listed), `IdentityDigitalTwinExporter` (tenant-filtered, no fingerprints/secrets, SHA-256). Migration `0148_identity_governance.sql`: 5 append-only RLS tables (`identity_lifecycle_events`, `identity_devices`, `identity_timeline_events`, `identity_break_glass_requests`, `identity_risk_snapshots`) with `tenant_id` NOT NULL + RLS via `current_setting('app.tenant_id', true)`. Docs: `docs/identity/{GOVERNANCE,BREAK_GLASS,DEVICE_TRUST,DIGITAL_TWIN}.md`. **144 tests** across 10 files (`tests/identity_governance/`), 212 total when combined with existing 68 authority tests. No new FastAPI routes. No changes to `api/auth_dispatch.py`. All CI gates pass. |

| PR 535 | **Enterprise Actor Attribution & Non-Repudiation** | 🔄 open (feature/pr-535-actor-attribution-non-repudiation) | Every governance action attributable to a real actor. Immutable identity snapshots at action time. Three-layer cryptographic fingerprint chain (actor_fingerprint → identity_fingerprint → request_fingerprint → attribution_hash → event_hash). Five deterministic read endpoints. 10 actor types including autonomous_system (infrastructure-only — no runtime AI). Privacy-safe IP/email/UA hashing. Prometheus attribution metrics. 27 tests (AA-1 through AA-27). `actor.read` / `actor.write` scopes. Tenant-isolated. Replay-safe. |
| PR feat/identity-assurance-trust-engine | **Enterprise Identity Assurance & Trust Levels** | 🔄 open (feat/identity-assurance-trust-engine) | Next stage of the Evidence Trust Chain: deterministic assurance level ladder (UNVERIFIED → PASSWORD → PASSWORD_MFA → SSO → SSO_MFA → CERTIFICATE → HARDWARE_KEY → WORKLOAD_IDENTITY, plus SERVICE_ACCOUNT and SYSTEM_AUTONOMOUS categories) with a 0–100 trust score and 5 trust bands (CRITICAL/LOW/MODERATE/HIGH/VERY_HIGH). Provider adapters for Keycloak, Entra ID, Okta, Google Workspace, Ping, Auth0. Immutable `AssuranceDecision` with SHA-256 fingerprint + monotonic hash-based `computed_at_sequence` (no wall clocks). Append-only `actor_assurance_snapshots` + `actor_assurance_history`; upsertable `actor_trust_metrics`; mutable-current `actor_identity_assurance`. Migration 0153 with RLS + append-only triggers. 5 endpoints (`GET /actor-assurance/{actor_id}`, `/history`, `/snapshot`, `/trust`, `POST /actor-assurance/recalculate`). `assurance:read` / `assurance:write` scopes. Prometheus assurance metrics. 177 tests (IA-1 through IA-177). Tenant-isolated. Deterministic — idempotent recalculation. |

### Active Security Hardening PRs (PR-SIGN series)

| PR | Item | Status | Deliverable |
|---|---|---|---|
| PR 415 | PR-SIGN-3/4/5: Billing HMAC hardening, startup validation, ingest signing | ✅ merged (#415) | `_attest()` fails closed without `FG_BILLING_EVIDENCE_HMAC_KEY`; startup validator errors in prod on missing signing/billing keys; Ed25519 signing headers on ingest report export |
| PR 416 | PR-SIGN-5b: Persistent ingest report signature metadata | 🔄 open | Six nullable columns on `ReportRecord` (`signature`, `signature_algorithm`, `signature_key_id`, `signed_at`, `signature_payload_hash`, `signature_version`); `_persist_report_signature()` called at finalization; export prefers persisted signature; migration 0104; 18 tests |

### Evidence Provenance PRs

**Moat arc:** 1.1 (foundation) → 1.2 (chain integrity) → 1.3 (authority) → 1.4 (report linkage) → 1.5 (enforcement) → 1.6–1.8 (trust graph) → 1.9 (proof packages) → 1.10 (trust replay)

| PR | Item | Status | Deliverable |
|---|---|---|---|
| PR 417 | PR 1.1: Evidence Provenance Foundation | 🔄 open | `fa_evidence_provenance` table (30 cols, ORM-managed); append-only RLS + triggers (migration 0105); provenance service with sanitize/hash/create/list/review/verify; wired into `create_evidence_link_route`; 22 tests |
| PR 418 | PR 1.2: Full Chain Replay Verification | 🔄 open | `services/field_assessment/trust_replay.py`: `verify_full_provenance_chain()` walks latest → genesis iteratively (O(n)); detects hash_mismatch, broken_link, cycle, duplicate_event_hash, tenant/engagement contamination, corrupt_genesis; result: `chain_valid`, `chain_depth`, `genesis_hash`, `latest_hash`, `verified_at`, `verification_duration_ms`, `chain_replay_score` (100/75/50/0), per-node lists, `verification_manifest_hash`; `verify_hash_chain()` generic reusable primitive; `generate_chain_verification_manifest()`; `ChainNodeData.signature_meta` reserved for PR 1.3; no new migrations; 40 tests; fg-fast passes |
| — | PR 1.2A: Trust Replay Proof Authority Hardening | 🔄 open | `replay_hash` (deterministic SHA-256 of outcome, excludes `verified_at`/`verification_duration_ms`); `replay_manifest_version = "trust-replay-v1"`; `replay_summary` dict (`verified_node_count`, `failed_node_count`, `warning_count`, `chain_depth`, `chain_replay_score`); `generate_trust_proof()` stub (shaped for PR 1.3 Ed25519 signing — no signing fields here); `engagement_id` in all result paths; `generate_chain_verification_manifest()` no longer makes a second DB query; 53 tests; fg-fast passes |
| — | PR 1.3: Evidence Authority Foundation | 🔄 open | `services/field_assessment/evidence_authority.py` (new): Ed25519 signing over `canonical_provenance_event` (immutable identity fields only — excludes mutable review/status); `sign_new_provenance_event()` pre-INSERT pattern (append-only trigger blocks post-flush UPDATE); `verify_provenance_signature()` returns `{valid, status, reason, authority_version, signing_key_id}`; `legacy_unsigned` (valid=None) ≠ failure; key-id fingerprint `SHA256(pub)[:16]`; `evidence_provenance.py` auto-signs on `create_evidence_provenance()` + `mark_provenance_reviewed()`; `_try_sign_new_event()` fail-closed in prod, silent-skip in dev/test; replay engine: `legacy_unsigned` → `SCORE_DEGRADED (50)`, `invalid_signature` → `SCORE_BROKEN (0)`; migration 0106 (5 nullable cols, no backfill); 33 new tests + 3 updated trust_replay tests; fg-fast passes (108/108) |
| — | PR 1.4: Evidence-to-Report Link Authority | 🔄 open | `fa_evidence_report_links` join table (20 cols, append-only, RLS-isolated); Ed25519-signed link events (`evidence-report-authority-v1`, `report-link-v1`); `build_canonical_report_link_event()` covers `event_hash`, `report_hash`, `report_signature`, `signing_key_id`, `authority_version`, `link_version`; `create_report_link()` pre-INSERT signing; `verify_report_links_bulk()` O(n) in-process bulk verification (no N+1 queries); trust replay extended: `linked_reports`, `verified_report_links`, `invalid_report_links`, `report_link_status`; legacy: no links = `unlinked`; migration 0107 (7 indexes, RLS, append-only triggers); 50 tests; fg-fast passes |
| — | PR 1.5: Trust Enforcement Authority | 🔄 open | `FG_PROVENANCE_MODE` (off/warn/strict, default=warn); `TrustInputs`+`TrustDecision` dataclasses; deterministic trust score 0–100 (100=trusted, 75=legacy-unsigned, 50=replay, 25=link, 0=chain/sig/tenant/engagement failure); five enforcement functions (`evaluate_trust_state`, `enforce_provenance_integrity`, `enforce_evidence_authority`, `enforce_report_link_authority`, `enforce_full_trust_chain`); `TrustEnforcementError` raised in STRICT mode; `FG_ALLOW_LEGACY_UNSIGNED` for legacy record policy; 5 Prometheus metrics (cardinality-safe, no tenant label); audit events (`trust_validation_passed/warning/blocked`, `trust_chain_failure`, `authority_failure`, `report_link_failure`); `services/field_assessment/trust_enforcement.py`; metrics in `api/observability/metrics.py`; 92 tests; all CI gates pass |
| — | PR 1.5A: Trust Enforcement Integration Layer | 🔄 open | `services/field_assessment/trust_enforcement_adapter.py` (new): 6 adapter functions (`enforce_evidence_creation`, `enforce_evidence_review`, `enforce_evidence_approval`, `enforce_report_finalization`, `enforce_report_export`, `enforce_trust_replay`); `_trust_inputs_from_replay_result()` converts replay result dict → TrustInputs; `_run_gate()` helper emits metrics on both allow and block paths; 4 new Prometheus counters (`frostgate_trust_enforcement_operations_total/allowed/warned/blocked_total`); wired into `evidence_provenance.py` (replaced manual TrustInputs assembly), `trust_replay.generate_trust_proof()`, `reports_engine.finalize_report()`, `field_assessment.export_engagement_report_route()`, `field_assessment.qa_approve_report_route()`; TrustEnforcementError surfaces as HTTP 422/403; 81 new integration tests; all CI gates pass |
| — | PR 1.6: Trust Graph Foundation | 🔄 open | `services/field_assessment/trust_graph.py` (new): in-memory trust graph substrate; 6 typed node types (Evidence, Finding, Control, Framework, Risk, Report); 6 typed edge types; adjacency-list traversal O(V+E); `verify_trust_graph()` detects missing/orphaned/invalid/cross-tenant/cross-engagement/cyclic/duplicate/replay violations; `get_{evidence,finding,control,risk,report}_lineage()` BFS traversal; `generate_trust_path()` deterministic BFS path; `generate_trust_graph_manifest()` SHA-256 canonical hash; replay-compatible (created_at on all nodes/edges); future-safe generic payload model; 117 tests; all CI gates pass |
| — | PR 1.6A: Trust Graph Authority & Snapshot Foundation | ✅ merged (#425) | `services/field_assessment/trust_graph_authority.py` (new): Ed25519 authority layer over the trust graph; `sign_edge_authority()` / `verify_edge_authority()` — per-edge Ed25519 signing over canonical JSON (fail-closed, never raises); `generate_signed_graph_snapshot()` / `verify_graph_snapshot()` — immutable signed graph state records (snapshot_hash excludes timestamps → identical structure produces identical hash); `build_replay_anchor()` — deterministic `{graph_hash, snapshot_hash, snapshot_signature, snapshot_version}` consumed by PR 1.9 without redesign; `why_report()` / `why_risk()` / `why_control()` / `why_finding()` — pure deterministic traversal-based trust explainability (no AI/inference); `TrustQueryResult` dataclass with `confidence=100` placeholder for PR 1.7; key management: `FG_EVIDENCE_SIGNING_KEY_B64` (private), `FG_EVIDENCE_VERIFY_KEY_B64` (verify-only mode); `signing_key_id = SHA256(pub_bytes)[:16]` rotation fingerprint; `TrustGraphAuthorityError(RuntimeError)` fail-closed; 132 tests |
| — | PR 1.6B: Trust Graph Authority Hardening | ✅ merged (#426) | `verify_replay_anchor()` independently verifiable proof object (sig check + snapshot field cross-validation + live graph freshness); `snapshot["graph_hash"]` P1 fix — recomputed manifest cross-check closes tampered_graph_hash gap; `edge_id` P2 fix — included in canonical Ed25519 payload for all three authority functions (build/sign/verify) preventing replay from one edge instance onto another; `TrustQueryResult.subject_id` / `.query_type` metadata fields + `to_dict()` inclusion; `EDGE_AUTHORITY_VERSION` in all canonical payloads with downgrade detection; 185 total tests (53 addendum: TestSnapshotSignatureInvariant, TestVerifyReplayAnchor, TestEdgeAuthorityVersionInPayload, TestTrustQueryResultExtended, TestNoPrivateKeyInOutput, TestMalformedInputs, TestLargeGraphPerformance — all 1000-node ops < 500ms) |
| — | PR 1.7: Trust Confidence & Corroboration Engine | ✅ merged | `services/field_assessment/trust_confidence.py` (new): deterministic confidence scoring engine (no AI/ML/randomness); `CONFIDENCE_VERSION = "trust-confidence-v1"`; 5 confidence levels (critical/weak/moderate/strong/high_assurance) + 5 strength levels (very_weak/weak/moderate/strong/verified); `_DECAY_TABLE` 6-tier freshness decay (0–30d: 0, 31–60d: −5, 61–90d: −10, 91–120d: −15, 121–180d: −20, 181+d: −25); all factor weights in `_POS`/`_NEG` named constants; 8 public functions: `calculate_confidence_decay` (date-driven tier lookup), `evaluate_evidence_strength` (signed vs unsigned, trust_score tiers, edge authority), `evaluate_corroboration` (source diversity: 1→20/2→40/3→60/4→75/5+→90+; duplicate detection −5 per same-source same-event_hash), `evaluate_trust_quality` (positive/negative factor enumeration with sorted output), `calculate_confidence` (full composite score 0–100 with all factors), `why_confidence` (+/−-prefixed explanation list), `replay_confidence` (point-in-time historical sub-graph reconstruction with `created_at ≤ at` filtering; fail-closed on cross-tenant/cross-engagement), `generate_confidence_manifest` (SHA-256 over canonical scores, timestamp excluded — identical scores → identical hash); `TrustConfidenceError(ValueError)`; 152 tests across 18 classes (determinism, cross-tenant isolation, performance — 1000 calcs < 500ms, AGI governance compatibility, tamper detection) |
| — | PR 1.7A: Confidence Authority & Drift Intelligence | ✅ merged | `services/field_assessment/confidence_authority.py` (new): Ed25519 authority layer over confidence scores; `CONFIDENCE_AUTHORITY_VERSION = "confidence-authority-v1"`; `ConfidenceAuthorityError(RuntimeError)`; 8 public functions: `sign_confidence_manifest()` / `verify_confidence_manifest()` — Ed25519 signing over canonical manifest JSON (fail-closed, never raises on verify); `generate_confidence_snapshot()` / `verify_confidence_snapshot()` — immutable signed confidence state records (snapshot_hash excludes snapshot_id/created_at → identical confidence state → identical hash); `calculate_confidence_drift()` — 5 directions (rapidly_improving/improving/stable/degrading/rapidly_degrading), 5 velocities (minimal/low/moderate/significant/rapid), inclusive threshold boundary at ±10; `generate_confidence_timeline()` — O(n log n) stable sort by (created_at, snapshot_id), supports 10,000+ snapshots; `generate_confidence_explainability_graph()` — Unicode tree with ├─/└─ connectors, auditor-readable section grouping; `TrustPolicy` dataclass + `evaluate_trust_policy()` — 8 policy names (evidence_approval/qa_approval/report_finalization/report_export/ai_deployment/agent_approval/agent_execution/agent_autonomy), extensible subject_type for future AGI entities; `replay_confidence_snapshot()` — 4 validation layers (snapshot_located/snapshot_authority/manifest_authority/graph_authority), fail-closed; `detect_confidence_anomalies()` — 7 anomaly types (confidence_collapse/confidence_inflation/authority_downgrade/corroboration_collapse/replay_degradation/signature_loss/trust_score_manipulation); key management reuses `FG_EVIDENCE_SIGNING_KEY_B64`/`FG_EVIDENCE_VERIFY_KEY_B64`; cross-tenant/cross-engagement fail-closed; 224 tests across 20 classes |
| — | PR 1.8: Trust Intelligence Layer | ✅ merged | `services/field_assessment/trust_intelligence.py` (new): intelligence layer above all prior trust authorities; `TRUST_INTELLIGENCE_VERSION = "trust-intelligence-v1"`; 10 public functions: `calculate_trust_posture()` — 6 posture levels (critical/degraded/watch/stable/healthy/excellent) from 5-component weighted composite (confidence 50%, replay 20%, graph 15%, authority 10%, enforcement 5%) with drift modifier; `calculate_trust_trend()` — 5 directions, 5 velocities, 4 windows (30/90/180/365d); `generate_trust_priorities()` — 12 candidate sources ranked by trust_delta, deduplicated, re-numbered; `calculate_trust_risk()` — 7 categories (authority/replay/graph/confidence/drift/governance/future_autonomy_risk), overall = max×0.70 + avg×0.30; `generate_trust_insights()` — deterministic insights sorted critical-first; `detect_trust_hotspots()` — 6 areas (evidence/authority/replay/graph/corroboration/governance); `generate_executive_actions()` — board-ready actions with 4 priority tiers and audience tags; `generate_governance_recommendations()` — entity_type extensible to human/agent/autonomous_system/agi/any; `forecast_trust_posture()` — linear extrapolation with 20%/35% dampening for 180d/365d uncertainty; `generate_trust_intelligence_graph()` — nodes+edges graph with typed IDs, tenant/engagement scoped; all 30+ constants exported (auditor visible); no ML/LLM/randomness; 270 tests across 20 classes |
| — | PR 1.8A: Trust Intelligence Authority & Historical Replay | ✅ merged | `services/field_assessment/trust_intelligence_authority.py` (new): persistence, replay, and historical authority layer above PR 1.8; `TRUST_INTELLIGENCE_AUTHORITY_VERSION = "trust-intelligence-authority-v1"`; `LEDGER_GENESIS_HASH = "0"*64`; `TrustIntelligenceAuthorityError(RuntimeError)`; 11 public functions: `generate_trust_intelligence_snapshot()` — immutable signed snapshot (snapshot_hash excludes snapshot_id/created_at; identical intelligence state → identical hash); `sign_trust_intelligence_snapshot()` — Ed25519 resign on deserialized snapshot; `verify_trust_intelligence_snapshot()` — fail-closed 6-check verification (fields, authority version, hash, signature, key_id); `replay_trust_intelligence()` — 6-layer validation (snapshot_located/snapshot_integrity/snapshot_signature/graph_integrity/confidence_integrity/authority_integrity), replay_score 0–100; `generate_trust_memory()` — historical timeline filtered to window (30/90/180/365d presets), posture/trend/risk histories, future snapshots excluded; `calculate_trust_evolution()` — explains trust change over time (major/moderate/minor improvement/regression, largest_improvements, largest_regressions, root_causes from direction/level/score changes); `compare_trust_snapshots()` — intelligence diff engine (trust_delta, added/removed risks by category score ≥50, improved/degraded controls by posture+trend+risk rank); `generate_decision_memory()` — governance decision records with entity_type extensible to human/agent/autonomous_system/agi/any future string; `generate_executive_timeline()` — board/investor/auditor/GovCon-readable events with impact strings; `generate_trust_ledger()` — append-only hash-chained ledger (LEDGER_GENESIS_HASH genesis, previous_hash linking, dedup by snapshot_hash, chronologically ordered); `verify_trust_ledger()` — chain integrity check (entry hash recompute, genesis sentinel, chain link); DB: migration 0108 — 3 append-only RLS tables (`fa_trust_intelligence_snapshots`, `fa_trust_intelligence_ledger`, `fa_trust_decision_memory`), 21 indexes total, append-only triggers; 315 tests across 22 classes |
| — | PR 1.8B: Evidence Trust Graph | 🗓 planned | Unified trust authority: Evidence Authority + Report Authority + Identity Authority + RBAC Authority in one traversable graph; at this layer FrostGate is trust infrastructure, not an assessment platform |
| — | P0-6A: Trust Arc Persistence & Delivery Foundation | ✅ done 2026-06-14 | Activated dormant trust arc infrastructure (migrations 0108/0109 tables existed, no ORM/callers/routes). `api/db_models_trust_arc.py`: 4 ORM models (`FaTrustIntelligenceSnapshot`, `FaAuditorProofPackage`, `FaTrustCertification`, `FaTrustDecisionMemory`), append-only. `services/trust_arc/orchestrator.py`: non-blocking orchestration (`generate_and_persist_trust_arc()` + `persist_decision_memory()`; absent signing key → `{skipped: True}`; caller owns tx). `api/trust_arc.py`: 4 delivery routes under `/field-assessment/engagements/{engagement_id}/trust-arc/` — `GET intelligence-snapshot`, `GET proof-package`, `GET certification` (all `governance:read`), `POST rebuild` (`governance:write`, governance/admin-only). Wired into `bundle_service.py` (trust arc on every bundle generation) and `qa_approve_report_route` (decision memory on QA approval). 4 new routes registered; route inventory + contract authority refreshed. All CI gates pass. |
| — | P0-6B: Trust Arc Enforcement Activation, Replay Validation & Authority Gates | ✅ done 2026-06-14 | Replaced advisory/default trust behavior with real runtime validation. `derive_engagement_trust_inputs(db, *, tenant_id, engagement_id) -> TrustInputs` added to adapter: queries latest provenance record, calls `verify_full_provenance_chain()`, maps result through `_trust_inputs_from_replay_result()`; fail-closed (no chain → all-False). Three enforcement call sites wired to derived inputs: `enforce_report_finalization()` in `api/reports_engine.py`; `enforce_evidence_approval()` in `api/field_assessment.py` (QA approval, signature_valid from report sig, chain/link/replay from provenance); `enforce_report_export()` in `api/field_assessment.py` (report export, same pattern). Bundle manifest extended with `trust_enforcement` section (derived fields included before `manifest_hash` computation; non-blocking). New CI guardrail `tools/ci/check_trust_enforcement_inputs.py` (AST check — 3 call sites must pass explicit chain_valid/link_valid/replay_valid; added to `security-regression-gates`). 5 new tests for `derive_engagement_trust_inputs()`. All CI gates pass. |
| — | P0-12: Federation Token Signing Enforcement | ✅ done 2026-06-16 | P0 security fix: `services/federation_extension/service.py` rewritten — `validate_token()` now performs full RS256 JWKS-backed cryptographic signature verification via PyJWT. New: `FederationValidationError(error_code, reason)` typed exception; `FederationPrincipal(subject, issuer, tenant_id, groups, raw_claims)` frozen dataclass; `FEDERATION_ALLOWED_ALGORITHMS = frozenset({"RS256"})` (alg=none and HS256 rejected before JWKS lookup); `FEDERATION_CLOCK_SKEW_SECONDS` (default 60s); kid-miss cache refresh (one retry); required claims: sub, exp, iss, aud; tenant_id/tid claim required post-verification. `JWKSCache.get()` extended with `force_refresh=False`. `api/auth_federation.py` updated to use `FederationPrincipal`. **21 tests** in `tests/security/test_federation_signing.py` (6 positive: RS256 valid, groups, clock skew, tid fallback, kid refresh, aud list; 15 negative: alg=none, HS256, tampered payload, missing exp, missing sub, expired, nbf, wrong issuer, wrong audience, missing aud, unknown kid, unauthorized key, missing iss, missing tenant, unconfigured service). No network in tests — local RSA key generation. All CI gates pass. |
| — | P0-11: Continuous Governance Control Tower (CGCT) | ✅ done 2026-06-16 | Read-only orchestration/aggregation layer above all existing authority systems (Trust Arc, TIM, CLM, QTB, Decision Ledger, Verification Bundles, Timeline). No new engines. **`api/control_tower.py`**: 14 routes under `/control-tower` (owned by existing `control` plane). **`services/cgct/`**: `posture.py` (weighted composite: trust×0.35 + cert×0.25 + evidence×0.25 + risk×0.15; 5 health levels; persisted to `fg_cgct_posture_snapshots`), `action_queue.py` (CLM cert actions, TIM open drift actions, bundle tamper/coverage actions), `aggregators.py` (drift, risk, evidence, timeline, decisions, certifications, executive view, governance graph, authority matrix). **Migration 0116**: 3 append-only RLS tables (`fg_cgct_posture_snapshots`, `fg_cgct_action_queue`, `fg_cgct_graph_edges`). **10 ENTERPRISE capabilities**: controltower.read/executive/risk/certification/evidence/timeline/decisions/drift/operations/admin. All CI gates pass. |
| — | P0-9: Quarterly Trust Briefs (QTB) | ✅ done 2026-06-15 | Executive deliverable layer above P0-7 TIM and P0-8 ETCC. No new trust engines — all report data sourced from existing P0-6A/B, P0-7 append-only tables. **Migration 0114**: 3 new tables — `fa_qtb_briefs` (status-mutable workflow record), `fa_qtb_brief_sections` (append-only content), `fa_qtb_brief_manifests` (append-only audit anchor). **`services/quarterly_briefs/brief_service.py`**: `generate_quarterly_brief()` (6-section full QTB: posture, drift, certification, governance, evidence, board_summary), `generate_board_brief()` (condensed board report). Deterministic hash chain: section_hash → brief_hash → manifest_hash → report_hash (SHA-256; same inputs produce same hashes). **`api/quarterly_briefs.py`**: 11 routes (POST generate/board/generate, GET list/get/manifest/export/board/history, POST review/approve). **5 new capabilities**: `trust.quarterly.briefs`, `trust.board.reporting`, `trust.report.export`, `trust.report.review`, `trust.report.delivery`. All ENTERPRISE tier. **75 unit tests** in `tests/test_quarterly_briefs.py`. quarterly_briefs_router registered in both app builders. All CI gates pass. |
| — | P0-8: Executive Trust Command Center (ETCC) | ✅ done 2026-06-15 | Executive governance layer above TIM and Trust Arc. No new trust engines — all data from existing producers. **4 new capabilities** in ENTERPRISE tier: `trust.executive.dashboard`, `trust.risk`, `trust.reporting`, `trust.executive.export`. **`services/executive_trust/posture_service.py`**: `get_executive_posture()` (aggregates FaTimTrustSnapshot + FaTrustCertification + open drift events into a single executive view), `get_tenant_overview()` (cross-engagement summary ordered by risk). **`api/executive_trust.py`**: 15 GET routes under `/field-assessment/etcc/` (tenant-level) and `/field-assessment/engagements/{id}/etcc/` (per-engagement) — posture, trends, risks, certification, certification/history, monitoring, timeline, decisions, drilldown/drift/{id}, drilldown/certification/{id}, reports/summary, reports/quarterly, reports/drift, reports/risk. Every widget traces to evidence: posture → drift event → source snapshot → certification. Quarterly report bounded by year+quarter params (P0-9 foundation). **Governance readiness**: actor_type schema (human/agent/workflow/system) already in all underlying tables; no redesign needed for autonomous governance. **35 unit tests** in `tests/test_executive_trust.py`. executive_trust_router registered in both app builders. All CI gates pass. |
| — | P0-7: Trust Intelligence Monitoring (TIM) Foundation | ✅ done 2026-06-14 | Activated customer-visible continuous trust monitoring on top of existing Trust Arc infrastructure (P0-6A/B). No new trust engines — all scores from existing producers. **Migration 0113**: `fa_tim_trust_snapshots` + `fa_tim_drift_events` — both append-only with RLS (app.tenant_id GUC), DB-level append-only triggers, indexes by tenant+engagement+status+severity. **3 missing ORM models** added to `api/db_models_trust_arc.py` (`FaTrustIntelligenceLedger`, `FaDecisionReconstructionRecord`, `FaChainOfCustodyRecord`; SQL `metadata` column mapped as `custody_metadata` to avoid SQLAlchemy reserved-name conflict). **`api/db_models_tim.py`**: `FaTimTrustSnapshot` + `FaTimDriftEvent`. **`services/trust_monitoring/`** (new package): `timeline_emitter.py` (6 non-blocking emit functions → existing `governance_timeline_events`); `drift_service.py` (7 deterministic drift rules: score_degradation/cert_expiration/cert_expired/evidence_staleness/replay_failure/missing_bundle/consecutive_degradation); `snapshot_service.py` (aggregates FaTrustIntelligenceSnapshot + FaTrustCertification + FaEvidenceProvenance count + FaVerificationBundle into FaTimTrustSnapshot, computes drift direction/score vs. previous TIM snapshot); `monitoring_engine.py` (`evaluate_and_persist_tim()` orchestrates snapshot → drift → timeline, non-blocking). **`api/trust_monitoring.py`**: 5 executive dashboard routes (governance:read + continuous.monitoring/ENTERPRISE): GET posture/timeline/drift/certification-status/risks. **Wiring**: trust arc orchestrator calls TIM evaluation after every trust arc activation; bundle service emits `verification_bundle_generated` timeline event on every bundle persist; `trust_monitoring_router` registered in both app builders; `api.db_models_tim` imported in db.py. **40 unit tests** in `tests/test_trust_monitoring.py`. All CI gates pass. |
| — | PR 1.9: Auditor Proof Package & Enterprise Trust Certification Authority | ✅ merged | `services/field_assessment/auditor_proof_authority.py` (new): transforms verified trust into deliverable, defensible proof artifacts; `AUDITOR_PROOF_AUTHORITY_VERSION = "auditor-proof-authority-v1"`; `PROOF_GENESIS_HASH = "0"*64`; `AuditorProofAuthorityError(RuntimeError)` fail-closed; 11 public functions: `generate_auditor_proof_package()` — complete signed proof bundle (8 sections: evidence_summary/intelligence_summary/replay_validation/graph_integrity/confidence_assessment/trust_ledger/decision_memory/package_metadata; section_hashes dict bound into package_hash → Ed25519; identical state → identical hash); `generate_executive_trust_brief()` — board/investor plain-English narrative (never raises); `generate_regulator_package()` — framework-agnostic compliance proof (NIST CSF/AI RMF/ISO 42001/SOC 2/HIPAA/PCI DSS; readiness from posture score not hardcoded mappings); `generate_legal_defense_package()` — 7-question decision reconstruction with reconstruction_hash; `generate_machine_verification_bundle()` — third-party verifiable offline export (5 components: trust/ledger/proof/manifest/verification; requires_frostgate=False); `generate_trust_certification()` — deterministic composite scoring (0.7×trust + 0.3×confidence; 5 levels: bronze≥50/silver≥60/gold≥70/platinum≥80/enterprise≥90; verification_hash excludes cert_id/validity dates; 90-day validity); `generate_chain_of_custody()` — hash-linked custody chain (PROOF_GENESIS_HASH genesis; custody_hash computed over stable fields before assigning custody_id; 7 event types); `sign_proof_package()` — Ed25519 re-sign on deserialized package (raises on missing hash); `verify_proof_package()` — 6-check fail-closed verification (fields/authority/section_hashes/package_hash/signature/key_id; never raises; returns {valid, reason}); `replay_auditor_package()` — 7-layer replay engine (evidence_authority×20/replay_authority×15/graph_authority×15/confidence_authority×15/intelligence_authority×15/trust_ledger×10/decision_memory×10; evidence+intelligence required for valid=True); `generate_enterprise_export()` — 5 formats (json/pdf/html/manifest/machine_bundle; invalid format falls back to json); entity_type extensible (human/reviewer/approver/agent/agent_fleet/autonomous_workflow/autonomous_system/agi/any string); DB: migration 0109 — 4 append-only RLS tables (`fa_auditor_proof_packages`, `fa_trust_certifications`, `fa_decision_reconstruction_records`, `fa_chain_of_custody_records`), 23 indexes total, append-only triggers; 334 tests across 22 classes |
| — | PR 13.1: Remediation Management Foundation | ✅ done 2026-06-18 | Enterprise remediation authority layer. New bounded context `services/remediation/` (`engine.py`, `repository.py`, `schemas.py`). `api/db_models_remediation.py`: 2 tables — `remediation_tasks` (mutable corrective-action record with priority/status enums, task_metadata extension point, schema_version) + `remediation_task_audits` (append-only, no UPDATE/DELETE). `RemediationEngine`: 6 tenant-aware methods (`create_task`, `get_task`, `list_tasks`, `update_task`, `close_task`, `delete_task`) + `get_task_audit_trail()` for lifecycle reconstruction. `api/remediation.py`: 6 routes under `/remediation/tasks` (POST/GET/GET-list/PATCH/POST-close/DELETE; `governance:read`/`governance:write` scopes). Reference validation: `finding_id` + `assessment_id` must exist within same tenant (cross-tenant references impossible). Audit: every mutation emits `task_created/updated/closed/deleted` event with `old_state`/`new_state` snapshots; audit trail persists after task deletion (REM-20). 4 Prometheus counters: `frostgate_remediation_tasks_created/closed/updates/denials_total` (no tenant labels). Priority enum: critical/high/medium/low/informational. Extension points reserved in `task_metadata` for risk_acceptance, compensating_controls, SLA, governance refs. **74 tests** in `tests/test_remediation_engine.py` (REM-1 through REM-42; 100% deterministic). All CI gates pass. |
| — | PR 13.2: Remediation Status Workflow Engine | ✅ done 2026-06-18 | Authoritative 5-state lifecycle engine. `RemediationStatus` extended: OPEN → PLANNED → IN_PROGRESS → CLOSED (terminal); OPEN/PLANNED/IN_PROGRESS → ACCEPTED_RISK (terminal). `_ALLOWED_TRANSITIONS` dict + `_TRANSITION_EVENT` map centralise all workflow logic in `services/remediation/engine.py`. `validate_transition()` static method rejects forbidden transitions (OPEN→CLOSED, PLANNED→CLOSED, CLOSED→*, ACCEPTED_RISK→*) with `RemediationInvalidTransition`. `transition_status()` method: validates → applies → audits → metrics. Audit event types: `task_planned`, `task_started`, `task_closed`, `task_risk_accepted`. `reason` required for ACCEPTED_RISK; optional for others; stored in `remediation_task_audits.reason` (new nullable TEXT column — migration 0119). `close_task()` delegates through state machine (IN_PROGRESS → CLOSED only). 2 new routes: `POST /remediation/tasks/{task_id}/transition` (governance:write), `GET /remediation/tasks/{task_id}/allowed-transitions` (governance:read). 2 new Prometheus counters: `frostgate_remediation_status_transitions_total{from_status,to_status}` (bounded label cardinality: 5×5=25 max), `frostgate_remediation_invalid_transitions_total`. **74 tests total** (REM-1–REM-42; existing REM-3/5/13/15/20 updated for new state machine). All CI gates pass. |
| — | PR 13.3: Remediation Ownership, Due Dates & SLA Authority | ✅ done 2026-06-19 | Ownership, due date, and SLA accountability layer on top of the remediation engine. 9 new nullable columns on `remediation_tasks`: `assigned_user_id/email/display_name`, `assigned_at`, `due_date`, `sla_target_days`, `sla_breach_at`, `ownership_reason`, `last_assignment_change_at`. `SLA_DEFAULTS` by priority (critical=14d, high=30d, medium=60d, low=90d, informational=None). `SlaStatus` enum: ON_TRACK/AT_RISK/OVERDUE/CLOSED/ACCEPTED_RISK. AT_RISK threshold=80% of SLA period elapsed. SLA computed at creation from priority; `sla_breach_at` stored as ISO timestamp. Owner requirement enforced before PLANNED→IN_PROGRESS transition. `RemediationOwnershipError` for unassign-of-unowned and unassign-of-IN_PROGRESS tasks. 5 new Prometheus counters: `frostgate_remediation_assignments/reassignments/overdue_tasks/sla_breaches/unassigned_tasks_total`. 6 new routes: `GET /remediation/tasks/overdue`, `GET /remediation/tasks/unassigned` (before `/{task_id}` to avoid path conflict), `POST /remediation/tasks/{task_id}/assign`, `POST /remediation/tasks/{task_id}/unassign`, `POST /remediation/tasks/{task_id}/due-date`, `GET /remediation/tasks/{task_id}/sla`. Audit events: `task_assigned`, `task_reassigned`, `task_unassigned`, `task_due_date_changed`. `GET /remediation/tasks/{task_id}/audit` route added. Migration 0120 with idempotent backfill. **102 tests total** (REM-1–REM-70; REM-20/22/40/41 updated for owner requirement on in_progress). All CI gates pass. |
| — | PR 13.4: Client Portal Remediation Integration | ✅ done 2026-06-19 | Client-facing remediation portal layer. New bounded context `services/remediation_portal/` (`engine.py`, `repository.py`, `schemas.py`). `api/db_models_portal_remediation.py`: 3 tables — `portal_remediation_comments` (editable with audit), `portal_evidence_submissions` (immutable after insert, SHA256-deduped per task), `portal_remediation_audit_events` (append-only with DB triggers). `PortalRemediationEngine`: 7 tenant-scoped methods (`get_dashboard`, `get_task`, `add_comment`, `edit_comment`, `list_comments`, `submit_evidence`, `list_evidence`, `acknowledge_ownership`, `get_portal_audit`). `api/portal_remediation.py`: 9 routes under `/portal/remediation` (`governance:read`/`governance:write` scopes). `PortalTaskView` safe projection excludes `assigned_user_id/email`, `created_by`, `schema_version`, `task_metadata`. Evidence SHA256 deduplication per task (409 on duplicate). Ownership acknowledgement idempotent (200 on repeat). 5 Prometheus counters: `frostgate_portal_remediation_views/comments/evidence_uploads/owner_acknowledgements/overdue_views_total`. Migration 0121 with RLS and `append_only_guard()` triggers. **30 new tests** (REM-71–REM-100). All CI gates pass. |
| — | PR 13.5: Portal Input Hardening & Operational Safety | ✅ done 2026-06-19 | Input validation hardening for the client portal remediation API. Pagination (`limit`/`offset`) added to all three portal list endpoints (comments, evidence, audit). `SubmitEvidenceRequest.sha256` field `min_length=64, max_length=64` constraints removed — regex `^[a-f0-9]{64}$` enforces length so `PORTAL_SHA256_VALIDATION_FAILURES_TOTAL` fires for all invalid inputs including wrong-length hashes. `AddCommentRequest` and `EditCommentRequest`: whitespace-only body validator added. 4 new Prometheus counters: `frostgate_portal_validation_failures_total`, `frostgate_portal_sha256_validation_failures_total`, `frostgate_portal_metadata_rejections_total`, `frostgate_portal_comment_validation_failures_total`. **75 tests total** (REM-71–REM-121; 21 new tests REM-101–REM-121). No new routes, no auth changes, no migrations. All CI gates pass. |
| — | PR 13.6: Portal Abuse Protection & Rate Limiting | ✅ done 2026-06-19 | Fixed-window rate limiting for all 4 portal write operations. New bounded-context modules: `services/remediation_portal/rate_limit.py` (ABC + `MemoryPortalRateLimiter` with injectable clock, thread-safe counter, `preseed()`/`reset()` test hooks, module-level singleton + `_set_portal_rate_limiter()` override) and `services/remediation_portal/rate_policy.py` (`PortalOperation` enum, `PortalRatePolicy` dataclass, `resolve_portal_limits()` reads env vars at call time, subscription-tier multipliers: starter=1×/professional=2×/enterprise=5×/government=5×). Rate-limit key: `portal:rl:{tenant_id}:{client_id}:{operation}` — tenant+client isolated. `_actor()` updated to use `auth.key_db_id` (per-key unique DB id) over `key_prefix` (fixed string). `PortalRateLimitExceeded` exception added to schemas; `_check_rate_limit()` in engine commits throttle audit event before raising. API layer returns `JSONResponse(429)` with `{"error": "RATE_LIMIT_EXCEEDED", "retry_after_seconds": N}` + `Retry-After` header. 4 new audit event types: `portal_rate_limit_exceeded/comment_throttled/evidence_throttled/acknowledgement_throttled`. 4 new Prometheus counters: `frostgate_portal_rate_limit_hits/comment_throttles/evidence_throttles/acknowledgement_throttles_total`. **75 tests total** (REM-71–REM-145; 24 new tests REM-122–REM-145). No new routes, no new DB tables, no migrations. All CI gates pass. |
| — | PR 13.7: Remediation Audit History & Notification Authority | ✅ done 2026-06-19 | Unified audit timeline and notification engine. New bounded context `services/notifications/` (`schemas.py`, `channels.py`, `engine.py`): `NotificationEngine` with delivery state machine (pending → sent/failed → acknowledged), injectable channel backends (`NullNotificationChannel` default, `EmailNotificationChannel` outbox stub, `PortalNotificationChannel` stub), `_set_notification_channel()` for test isolation. `api/db_models_notifications.py`: `notifications` table (mutable, 3 compound indexes). `services/remediation/timeline.py`: `UnifiedTimelineEngine.get_timeline()` merges `remediation_task_audits` (source=remediation) + `portal_remediation_audit_events` (source=portal) + `notifications` non-pending (source=notification); filters by `event_type`/`source`/`since`/`until`; sorts by `event_at` ASC; paginated; raises `RemediationNotFound` on tenant violation. `TimelineEventResponse` + `TimelineListResponse` added to `services/remediation/schemas.py`. 2 new routes: `GET /remediation/tasks/{task_id}/timeline` (`governance:read`), `POST /remediation/tasks/{task_id}/notifications/{notification_id}/acknowledge` (`governance:write`). Notification hooks in `RemediationEngine`: `assign_owner` → `TASK_ASSIGNED`, `remove_owner` → `TASK_UNASSIGNED` (captures old email before clearing), `transition_status` → `TASK_CLOSED`/`TASK_ACCEPTED_RISK` (conditional on `assigned_user_email`). 5 new Prometheus counters: `notifications_sent/failed/acknowledged_total`, `timeline_events_total`, `sla_escalations_total`. Migration `0122_notifications.sql` with RLS. **42 tests** (REM-149–REM-190; 41 passed, 1 skipped). All CI gates pass. **FA-TL-01 (future)**: current timeline implementation fetches all 3 sources in Python and merges in-memory — acceptable for typical task volumes (hundreds of events). If any task accumulates >10k timeline events, migrate to a materialized `remediation_timeline_events` table (single source, indexed by `tenant_id+task_id+event_at`) populated by DB triggers/a background worker; `UnifiedTimelineEngine` becomes a thin query wrapper. No action needed until volume metrics indicate the threshold is approached. |
| — | PR 14.3: Compensating Control Registry & Evidence Governance Foundation | ✅ done 2026-06-20 | New bounded context `services/control_registry/` with full governance rule enforcement. 5 ORM models: `control_registry`, `control_evidence_links` (append-only), `risk_acceptance_control_links`, `control_reviews`, `control_audits` (append-only). `ControlRegistryEngine` enforces 4 invariants: HIGHLY_EFFECTIVE requires VERIFIED; status transitions validated against FSM (`CONTROL_STATUS_TRANSITIONS`); evidence required before verification (`verify_control` raises if 0 evidence links); RETIRED blocks risk links. Freshness engine: FRESH (<50% of `review_frequency_days`), AGING (50–99%), STALE (100–149%), EXPIRED (≥150% or never verified). 16 routes: `/controls/dashboard` (aggregates), `/controls/maintenance/freshness` + `review-sweep` (autonomous sweeps), CRUD + `/verify` + `/evidence` + `/risk-links` + `/reviews` + `/reviews/{id}/complete` + `/audit`. Route ordering critical: literal paths before `/{ctl_id}`. `CONTROL_REGISTRY` added to `SourceType` enum + `TIMELINE_ADAPTERS`. 9 new `NotificationTrigger` values. 6 new Prometheus counters (bounded cardinality). `/controls` plane registered. Contracts + route inventory regenerated. **85 tests** (80 CCR-1–CCR-80 + 5 adapter completeness). All CI gates pass. |
| — | PR 14.5: Governance Reporting & Attestation Engine | ✅ done 2026-06-22 | New bounded context `services/governance_reporting/`. Deterministic governance evidence packages: report generation assembles risk section, approval chain, review history, compensating control evidence, and governance timeline into a SHA-256 hash-verified document. 4 new ORM tables: `risk_governance_reports`, `risk_governance_report_manifests` (one per report), `risk_governance_attestations` (append-only signoffs), `risk_governance_report_audits` (append-only trail). `GovernanceReportingEngine`: generate (supersedes previous COMPLETED reports for same risk, version increments), list, get, manifest, timeline, attest, verify (VALID/INVALID/TAMPERED), export-html, export-pdf (reportlab). Attestation supports OWNER/RISK_OWNER/APPROVER/REVIEWER/EXECUTIVE/AUDITOR types + HUMAN/AGENT/SYSTEM actor types for autonomous governance readiness. 10 routes under `/governance-reports`. `GOVERNANCE_REPORTING` added to `SourceType` + `TIMELINE_ADAPTERS`. 5 new `NotificationTrigger` values. 6 new Prometheus counters. **113 tests** (108 GR-1–GR-108 + 5 adapter completeness). All CI gates pass. |
| PR 14.6.6 | Verification Workflow Authority | 11-state workflow, assignment engine, escalation, SLA governance, queue authority, evidence integration | ✅ Complete | #TBD |
| PR 14.6.7 | Evidence Freshness Authority | New bounded context `services/evidence_freshness_authority/`. Freshness policies, per-evidence freshness records (6 states: CURRENT/DUE_SOON/REVIEW_REQUIRED/VERIFICATION_REQUIRED/STALE/EXPIRED), deterministic scoring (0-100), exception management, dashboard & CGIN snapshot, timeline integration (10 event types), verification authority integration, plane registration, 150+ tests. | ✅ Complete | #TBD |
| PR 17.6D | Governance Optimization Engine | New bounded context `services/governance_optimization/`. Consumes all prior governance authorities (learning, adaptive intelligence, chain, playbooks) to produce deterministic ranked optimization decisions. **Priority Formula**: `score = accuracy×60 + health_bonus + eff_bonus + size_bonus - failure_penalty - deprioritize_penalty`, clamped 0–100, fully auditable. **Recommendation Ranking**: ranks recommendation types by calibrated accuracy from GAI accuracy aggregates; deprioritizes types with `CALIBRATED_LOW` accuracy < 0.25. **Remediation Prioritization**: ranks remediation categories by weighted success rate from learning aggregates; includes `no_change_count` in all denominators. **Bridge Prioritization**: ranks governance chain bridges by failure rate — inverted (high failure = high priority score = rank 1 = needs most attention); high skip rate flagged as workflow gap. **Strategy Weighting**: ranks industry strategy profiles (HEALTHCARE/FINANCIAL/INSURANCE/GOVERNMENT/LEGAL/MSP/GENERAL) by playbook performance. **Control Prioritization**: graceful-degradation placeholder for control effectiveness data. **Aggregate Tracking**: per-target rolling stats (times_ranked, avg/latest/highest/lowest score) in `fa_governance_optimization_aggregates`. **Snapshots**: append-only per-run snapshots per optimization type in `fa_governance_optimization_snapshots`. **CGIN Snapshot**: per-type statistics + confidence distribution, `tenant_fingerprint = sha256("cgin:v1:{tenant_id}")[:32]` — no raw tenant_id. 3 new DB tables (migration 0141): `fa_governance_optimization_decisions` (append-only with ORM + PG trigger), `fa_governance_optimization_aggregates` (mutable), `fa_governance_optimization_snapshots` (append-only). 15 routes under `/governance-optimization` in `control` plane. **95 tests** (90 unit + 5 E2E). All CI gates pass. | ✅ Complete | #TBD |
| PR 17.6C | Governance Adaptive Intelligence Authority | New bounded context `services/governance_adaptive_intelligence/`. Transforms Governance Learning into Adaptive Governance Intelligence by tracking recommendation outcomes and calibrating confidence from historical correctness. **Recommendation History**: `fa_governance_recommendation_history` — append-only, one row per status transition; `recommendation_id` groups transitions; latest-row-per-recommendation is current state. Status lifecycle: PENDING → ACCEPTED/REJECTED → EXECUTED → CLOSED. **Outcome Tracking**: `fa_governance_recommendation_outcomes` — records health/effectiveness/verification/freshness/forecast deltas + `success` bool; UNIQUE on `(tenant_id, recommendation_history_id)`. **Accuracy Aggregates**: `fa_governance_accuracy_aggregates` — per-type: `accuracy_score = successful / executed`; calibrated confidence CALIBRATED_HIGH (≥0.75) / CALIBRATED_MEDIUM (≥0.50) / CALIBRATED_LOW (≥0.25) / CALIBRATED_UNKNOWN. **Playbook Engine**: `fa_governance_playbooks` — per-type: `success_rate`, `avg_health_improvement`, `sample_size`; rebuilt by `recalculate`. **Adaptive Recommendations**: `generate_adaptive_recommendations()` consumes both learning aggregates AND accuracy aggregates — poor-performing recommendation types (accuracy < 0.25) are automatically deprioritized. **Strategy Profiles**: 7 static industry profiles (HEALTHCARE/FINANCIAL/INSURANCE/GOVERNMENT/LEGAL/MSP/GENERAL) from `strategy_profiles.py`. **CGIN Snapshot**: `tenant_fingerprint = sha256("cgin:v1:{tenant_id}")[:32]`; recommendation_accuracy, confidence_distribution, playbook_statistics — no raw tenant_id. 4 new DB tables (migration 0140) + RLS on all tables. 15 routes under `/governance-adaptive-intelligence` in `control` plane. **139 tests** (132 unit + 7 E2E). All CI gates pass. | ✅ Complete | #TBD |
| PR 17.6B | Governance Learning Loop Authority | New bounded context `services/governance_learning/`. Closes the governance intelligence loop by ingesting remediation outcome events and producing deterministic recommendations. **Outcome Ingestion**: `ingest_outcome` writes an immutable `fa_governance_learning_records` row and upserts per-category running aggregates in `fa_governance_learning_aggregates`. `LearningSignal` detection: IMPROVES_EFFECTIVENESS, HIGH_SUCCESS_RATE, HIGH_FAILURE_RATE, REPEATED_FAILURE, STABILITY_LOSS, CONFIDENCE_GAP — all deterministic from aggregate state. **Scoring**: `compute_success_score` (clamp(50 + score_delta×2, 0, 100)), `compute_confidence_score` (starts 100, reduced by missing inputs, failure ratio, skip ratio). **Classification**: `classify_momentum` (ACCELERATING/IMPROVING/STABLE/DECLINING/STALLING), `classify_stability` (STABLE/VARIABLE/VOLATILE), `classify_confidence` (HIGH/MEDIUM/LOW/VERY_LOW). **Deterministic Recommendations**: 3 rules in `generate_recommendations()` — prioritize best category, escalate worst, governance review on declining health. **CGIN Snapshot**: `get_cgin_snapshot()` returns `tenant_fingerprint = sha256("cgin:v1:{tenant_id}")[:32]` — raw `tenant_id` never exposed. 2 new DB tables (migration 0138): `fa_governance_learning_records` (append-only; ORM + PG trigger guards), `fa_governance_learning_aggregates` (mutable; UNIQUE on `(tenant_id, remediation_category)`). 10 routes under `/governance-learning` in `control` plane: `GET /dashboard`, `GET /learning-records`, `GET /aggregates`, `GET /recommendations`, `GET /top-performers`, `GET /top-failures`, `GET /momentum`, `GET /cgin/snapshot`, `POST /ingest-outcome`, `POST /recalculate`. **132 tests** (127 unit + 5 E2E). All CI gates pass. | ✅ Complete | #TBD |
| PR 17.6A | Governance Chain Completion | Converts remaining NOOP_SAFE/skipped bridges to real propagation. `ASSESSMENT_TO_EVIDENCE`: calls `EvidenceAuthorityEngine.create_evidence()`; idempotent (NOOP_SAFE if evidence already exists). `EVIDENCE_TO_VERIFICATION`: strengthened — evidence existence check (SKIPPED_UNAVAILABLE if not found), duplicate-active-request prevention (NOOP_SAFE if exists). `ACTION_TO_REMEDIATION`: real when `finding_id`+`assessment_id` provided; SKIPPED_UNAVAILABLE otherwise. `ALL_TO_REPORTING`: new bridge probing 5 authority tables; SKIPPED_UNAVAILABLE lists missing authorities; REPORT_GENERATED when all present. **Governance Health v2**: `governance_momentum` (direction of score change, 50=neutral), `governance_stability` (inverse of recent score variance), `governance_confidence` (starts 100, reduced by missing inputs + failure/skip ratios). Migration 0137 adds 3 nullable columns to `fa_governance_health_snapshots`. 2 new routes: `GET /governance-chain/health/history`, `POST /governance-chain/validate` (6-check integrity validator; returns PASS/WARNING/FAIL + findings; no mutations). **36 new/updated tests** (GC-213–GC-240 + GCE2E-01–GCE2E-08); **248 total pass**. All CI gates pass. | ✅ Complete | #TBD |
| PR 17.6 | Canonical Governance Chain Authority | New bounded context `services/governance_chain/`. Orchestrates existing governance authorities without replacing them. **Bridge Engine**: 8 bridge methods connecting assessment→evidence→verification→freshness→effectiveness→explainability→action→remediation→outcome; each bridge wraps downstream calls in try/except (non-blocking); returns `SKIPPED_UNAVAILABLE` or `NOOP_SAFE` when data is missing — no fake success. **Governance Health Foundation**: deterministic weighted average (verification 25%, freshness 25%, effectiveness 25%, remediation 15%, forecast 10%); computed from existing authority tables; defaults to 75.0 when no data (absence ≠ failure). **CGIN Anonymization**: `tenant_fingerprint = sha256("cgin:v1:{tenant_id}")[:32]` — raw tenant_id never exposed. 4 new DB tables (migration 0136): `fa_governance_chain_events`, `fa_governance_chain_executions`, `fa_governance_health_snapshots` (all append-only with ORM+PG guards), `fa_governance_chain_snapshots` (mutable CGIN cache). 8 routes under `/governance-chain` in control plane: `GET /health`, `GET /diagnostics`, `GET /cgin/snapshot`, `GET /executions`, `GET /executions/{id}`, `GET /events/{correlation_id}`, `POST /execute`, `POST /recalculate-health`. All require `governance:read` or `governance:write`. **212 tests** (GC-1–GC-212). All CI gates pass. | ✅ Complete | #TBD |
| PR 17.5 | Remediation Effectiveness Analytics Authority | New bounded context `services/remediation_effectiveness/`. Closes the feedback loop from remediation → measurement → learning. **Outcome Recording Engine**: deterministic per-remediation outcome recording; score delta classification (SUCCESS ≥10 / PARTIAL_SUCCESS ≥3 / NO_CHANGE ≥-3 / REGRESSION ≥-10 / FAILURE <-10); remediation effectiveness score = clamp(50 + delta×2.5, 0, 100); 5 effectiveness levels (HIGHLY_EFFECTIVE/EFFECTIVE/ADEQUATE/WEAK/INEFFECTIVE). **ROI Scoring**: headroom-relative ROI = delta / (headroom+1) × 100; normalized 0–100; 5 ROI classifications (EXCELLENT/GOOD/ACCEPTABLE/POOR/NEGATIVE). **Persistence Tracking**: append-only `fa_remediation_persistence` windows; SUSTAINED/HOLDING/DECLINING/LOST classification from delta vs. close score. **Pattern Detection Engine**: 6 pattern types (REPEATED_FAILURE/RECURRING_DEGRADATION/NO_IMPROVEMENT/ROLLBACK_PATTERN/CONSISTENT_IMPROVEMENT/RAPID_REGRESSION); 4 severity levels (CRITICAL/HIGH/MEDIUM/LOW); deterministic detection rules per control history. **Learning Aggregates**: per-category aggregate rebuild on recalculate; success_rate, average_score_delta, average_roi_score per RemediationCategory (9 categories). **Dashboard & CGIN Snapshot**: aggregate statistics across all outcomes; top/worst performing category; CGIN benchmark-ready snapshot. 4 new DB tables (migration 0135): `fa_remediation_outcome` (delete-protected), `fa_remediation_persistence` (append-only, UPDATE+DELETE blocked), `fa_remediation_learning` (mutable), `fa_remediation_pattern` (mutable). 11 routes under `/remediation-effectiveness`: `POST /`, `GET /dashboard`, `GET /patterns`, `GET /top-successes`, `GET /failures`, `GET /cgin/snapshot`, `POST /recalculate`, `GET /`, `GET /{id}`, `PATCH /{id}`. Route ordering enforced: multi-segment routes before `/{id}`. 4 Prometheus counters. **250+ tests** (REM-1–REM-250+). All CI gates pass. | ✅ Complete | #TBD |
| PR 18.7 | Autonomous Governance Operations Center (AGOC) | Route `/dashboard/operations-center`. MCIM authority `OPERATIONS-CENTER`. 10 panels: ExecutiveOperationsQueue (`/decisions`), GovernanceAutomationQueue (`/ui/forensics/events`), DecisionExecutionPipeline (`/decisions`, 10-stage), OperationalRiskHeatmap (`/governance/graph/stats + /anomalies`), EvidenceFreshnessMonitor (`/governance/graph/nodes`, trustScore + age), PolicyConflictCenter (`/anomalies`, 7 conflict types), GovernanceSLAMonitor (`/decisions`, SLA breach detection), AutomationSafetyCenter (`/control-tower/snapshot`, riskScore + killSwitch + blastRadius), CrossAuthorityTimeline (Forensics Chain + Event Feed, immutable + auditable), ExecutiveOperationalBriefing (composite, sufficientEvidence gate — fail closed). CI gate `tests/check_operations_center.py` (16 checks). Test suite `apps/console/tests/operations-center.test.js` (900+ assertions across 14 describe blocks). Architecture doc `docs/OPERATIONS_CENTER_18_7.md`. `operationsCenterApi.ts` API layer (10 functions, `LoadResult<T>` discriminated union). No fake metrics. No dangerouslySetInnerHTML. No localStorage governance state. Enterprise-ready. AGI-ready (actor_type extensible to autonomous_system/agi/agent_swarm). | ✅ Complete | pr/18.7-operations-center |
| PR 18.8.1 | Governance Digital Twin Foundation | Service-first deterministic foundation in `services/governance_digital_twin/`. Canonical snapshot model, tenant-scoped snapshot builder, explicit authority graph, stable entity/relationship linking, SHA-256 canonical JSON fingerprinting, replay-safe export, baseline creation, CI gate `tools/ci/check_governance_digital_twin.py`, deterministic tests, architecture doc `docs/GOVERNANCE_DIGITAL_TWIN_18_8_1.md`. API shell intentionally deferred until state model and safety contract stabilize. Enterprise-grade and replay-safe. | 🔄 In Progress | pr/18.8.1-governance-digital-twin-foundation |
| PR 18.8.2 | Governance Simulation Engine | `services/governance_simulation/`: deterministic scenario simulation, overlay engine, graph diff, impact analysis, executive comparison, replay packages, SHA-256 fingerprinting, CI gate `tools/ci/check_governance_simulation.py`, 200+ assertions. Digital Twin remains immutable. Simulation consumes snapshots and produces overlays only. | 🔄 In Progress | pr/18.8.2-governance-simulation-engine |
| PR 18.8.3 | Closed-Loop Governance Execution Engine | `services/governance_execution/`: execution planner, approval workflows, lifecycle state machine, verification engine, measurement engine, rollback planning, decision ledger, replay packages, SHA-256 fingerprinting, CI gate, 250+ assertions. Digital Twin and Simulation remain immutable. | 🔄 In Progress | pr/18.8.3-closed-loop-governance-execution |
| PR 18.1 | Enterprise Assessment Report Authority | New bounded context `services/report_authority/` (23 modules). `api/db_models_report_authority.py`: 3 tables — `fa_report`, `fa_report_audit_events` (append-only), `fa_report_bundles`. 15 routes under `/reports`. Deterministic PDF (ReportLab), HTML (offline-capable), JSON (canonical sort_keys) rendering. SHA-256+SHA-512 dual hashing, HMAC-SHA256 signing, immutable manifests with tamper detection. Signed ZIP export bundles with offline auditor verification package. Report Quality Score (5-factor weighted composite → EXCELLENT/GOOD/ACCEPTABLE/POOR/INCOMPLETE). Multi-version comparison. Regulator profiles (HEALTHCARE/FINANCE/LEGAL/GOVERNMENT/MANUFACTURING). Evidence Traceability Matrix. Enterprise branding layer. Plane registry updated. CI gate `tools/ci/check_report_authority.py`. Migration 0142. **348 tests** (5 files). All CI gates pass. | ✅ Complete | pr/18.1-report-authority |
| PR 18.1a | Enterprise Report Delivery (Approval + Delivery + Supersede Lineage) | 2 new ORM models on `api/db_models_field_assessment.py`: `FaReportVersion` (mutable through status transitions — draft/internal_review/approved/delivered/superseded/archived; guarded by route-layer immutability once status ≥ approved) and `FaReportDeliveryEvent` (append-only via PG `append_only_guard()` trigger). Migration `0150_report_versioning.sql` adds both tables, FK constraints for parent/superseded lineage, tenant indexes on `(tenant_id, engagement_id)`, `(tenant_id, report_id)`, `(tenant_id, status)`, row-level security policies. 9 new routes on the field-assessment router: `POST /engagements/{engagement_id}/reports/{report_id}/versions`, `GET .../versions`, `GET .../versions/{version_id}`, `POST .../versions/{version_id}/submit-for-review`, `POST .../versions/{version_id}/approve`, `POST .../versions/{version_id}/deliver`, `POST .../versions/{version_id}/supersede`, `GET .../versions/{version_id}/manifest`, `GET .../versions/{version_id}/history`. Deterministic `ReportManifest` — SHA-256 over sorted canonical JSON for both the underlying `GovernanceReportRecord.report_json` and the manifest itself; embeds evidence hashes drawn from `FaEvidenceReportLink`, finding IDs, control IDs, framework IDs, per-category counts. All routes require `governance:read`/`governance:write` + `require_bound_tenant()`. Every state transition emits a `FaReportDeliveryEvent` (`generated`/`reviewed`/`approved`/`downloaded`/`superseded`) AND a `FaEngagementAuditEvent`. **18 tests** in `tests/test_report_delivery.py`. No changes to existing report routes. Signed report row is untouched — versioning wraps the artifact, does not replace it. | ✅ Complete | feat/enterprise-report-delivery |
| PR 18.6.8a | Workspace Integration Hardening & Warning Cleanup | React hook stale-closure fixes in `executive/page.tsx` (ForecastTab + BoardSummaryTab — `useRef(Boolean(initialData))` guard, zero React exhaustive-deps warnings). Context safety: `sanitizeContext()` added to `workspaceContext.ts`; `mergeWorkspaceContext` now strips unknown keys and empty strings. CI gate hardened from 13 → 19 checks (exec page hooks, MapIterator spread, eval-route absence, nav route coverage, context filtering, demo mode safety). Test suite expanded 836 → 907 (71 AU-section assertions). Pre-existing infra fixed: 34 mypy errors suppressed via `mypy.ini`; unrecognised `asyncio_default_fixture_loop_scope` removed from `pytest.ini`; ruff unnecessary-f-string violation in CI script fixed. No backend, API, schema, or business logic changes. | ✅ Complete | pr/18.6.8a-workspace-integration-hardening |
| PR 18.6.8 | Enterprise Workspace Integration & Demo Readiness | Workspace integration layer connecting 11 enterprise workspaces (Executive Intelligence Center, Operations Workspace, Trust Center, Customer Portal, Assessment Workspace, Reporting Workspace, Readiness, Governance Intelligence, Remediation, Engagement Portal, Command Center). 8 new `workspace-integration` components (CrossWorkspaceNav, WorkspaceContextBridge, WorkspaceMetadata, WorkspaceEmptyState, WorkspaceLoadingState, DemoModeIndicator, WorkspaceSearch, index.ts). 3 lib utilities (`workspaceContext.ts`, `demoFixtures.ts`, `workspaceNav.ts`). URL-based context preservation for 15 context keys (tenant, engagement, assessment, report, finding, remediation, policy, decision, timelinePosition, framework, control, evidence, customer, simulation, replay). Deterministic demo fixtures (DEMO_MODE_ACTIVE=false, 5 engagements, 10 findings, 5 reports, 8 remediations). Cross-workspace navigation map covering 6 workspace entries. Navigation registry bumped to 18.6.8 with 4 new enterprise workspace entries. CI gate `check_workspace_integration.py`. Architecture doc `WORKSPACE_INTEGRATION_18_6_8.md`. 1000+ static-analysis tests. No backend changes. | ✅ Complete | pr/18.6.8-workspace-integration-demo-readiness |
| PR 18.6.7 | Executive Intelligence Center (Strategic Governance Command) | `api/executive_intelligence.py` — 10 deterministic endpoints under `/api/executive` (overview, posture, risk, compliance, business, trends, recommendations, forecast, priorities, summary). All metrics derived from compliance_findings, decisions, audit_ledger, compliance_requirements — zero fabricated analytics. OLS linear regression forecast with explicit R² confidence and limitations. Explainability envelope on every metric (source, calculation, evidence_ids, snapshot_ts, framework_mapping, confidence, authority). Duplicate router registration fixed in main.py. Executive page `/dashboard/executive` with 8-tab workspace (Overview, Risk, Compliance, Trends, Business, Recommendations, Forecast, Board Summary). Navigation registry updated (executive-intelligence, MCIM-18.6.7, roles: Executive/CISO/Auditor/Administrator). `executiveApi.ts` client with SafeResult pattern. 38 tests covering: severity weights, snapshot determinism, OLS math, priority scoring, cost calculation, router structure, tenant isolation. All 38 tests pass. | ✅ Complete | feat/pr-18.6.7-executive-intelligence-center |
| PR 18.6.6 | Enterprise Customer Portal Experience | 22 portal components (PortalShell + 13 core + 8 extended: NotificationCenter, SupportCenter, ObservationsPanel, AuditEventsLog, DocumentCenter, ScanHistoryPanel, QuestionnaireSummary, ComplianceOverview), 8 new pages (/dashboard /trust /timeline /actions /changes /export /notifications /support), CI gate `check_customer_portal.py` (31/31), 683 static-analysis tests (0 failures), architecture doc. P2 hardening: engagementStore security contract, localStorage constrained to approved UX-only patterns, admin/console route ban, fail-closed guards. All components MCIM-compliant with `customerSafe=true`. No backend changes. | ✅ Complete | pr/18.6.6-enterprise-customer-portal |
| PR 18.6.5 | Enterprise Trust Center | 19 new trust-center components (TrustCenterShell, TrustScorecard, ContinuousAssurancePanel, TrustEvidenceGraph, DecisionProvenanceExplorer, GovernanceReplayCenter, ChangeIntelligence, TrustCertificates, AuditReadinessWorkspace, CustomerTrustView, TrustTimeline, OperationalMemory, DecisionEffectiveness, BottleneckAnalysis, TrustBenchmarks, CaseRelationships, WorkspaceIntelligence, SLAForecasting, CommandCenterIntegration), trust-center page `/trust-center`, CI gate `check_trust_center.py`, 1000+ static-analysis tests, architecture doc. All panels MCIM-compliant. No backend changes. | ✅ Complete | pr/18.6.5-enterprise-trust-center |
| PR 18.6.4 | Enterprise Operations Workspace | 13 new workspace components (WorkspaceShell, UnifiedWorkQueue, CaseWorkspace, DecisionLedger, WorkflowProgress, InvestigationTimeline, CrossAuthorityNav, AuthorityHealthMap, CorrelationGraph2, CommandPalette, PlaybookPanel, DelegationPanel, ExportPanel), workspace page `/workspace`, CI gate `check_operations_workspace.py`, static-analysis tests. All panels MCIM-compliant with full authority declarations. No backend changes. | 🔄 In Progress | pr/18.6.4-enterprise-operations-workspace |
| PR 18.6.3 | Operations Workspace | 6 new React widgets (InvestigationDrawer, OperationalHealthMatrix, AuthorityMap, CorrelationGraph, ReplaySeam, FutureReservedPanels), 4 enhanced components (WidgetShell, ExecutiveBriefing, ExecutiveNotifications, DecisionProvenancePanel), 3 new dashboard sections (ops-matrix-heading, correlation-heading, future-heading), CI gate `check_command_center_authority.py`, 700+ static-analysis tests. No backend changes. | ✅ Complete | pr/18.6.3-command-center-actions |
| PR 18.6.2 | Executive Command Center | 18 command-center widget components, async server page.tsx with parallel data fetching, `check_executive_dashboard.py` CI gate, 511 static-analysis tests. No backend changes. | ✅ Complete | pr/18.6.2-command-center |
| PR 18.6.1 | Unified Navigation Framework | `@fg/navigation` package, sidebar from registry, portal nav from registry, JSON snapshot, `check_navigation_registry.py` CI gate, 399 tests. | ✅ Complete | pr/18.6.1-navigation-framework |
| PR 18.5A | Governance Intelligence Evidence Graph & Decision Provenance | Extension of Governance Intelligence Authority (PR 18.5). 10 new pure service modules: `provenance.py` (ProvenanceGraph DAG, build_node, compute_node_digest), `counterfactual.py` (9-scenario dispatch; all outputs `result_label="PROJECTED"`, `is_production=False`), `replay.py` (build_replay_snapshot, replay_governance; all outputs `replay_label="REPLAY"`, `is_production=False`), `evidence_matrix.py` (raises on empty evidence_ids), `quality_score.py` (5 grades; weights sum to 1.0), `benchmark_confidence.py` (MINIMUM_SAMPLE_SIZE=10, MINIMUM_COHORT_SIZE=5, FRESH/STALE/EXPIRED freshness), `timeline_diff.py` (8 supported windows), `simulation_compare.py` (comparison_label="DETERMINISTIC_COMPARISON", is_production=False), `evidence_impact.py` (10-stage IMPACT_CHAIN blast-radius), `export_package.py` (JSON/HTML/MANIFEST; no PDF; strips tenant_id recursively; SHA-256 hash). 9 new ORM models (`fa_gov_intel_provenance_node`, `fa_gov_intel_provenance_edge`, `fa_gov_intel_replay_snapshot`, `fa_gov_intel_evidence_matrix`, `fa_gov_intel_quality_score`, `fa_gov_intel_simulation_comparison`, `fa_gov_intel_timeline_diff`, `fa_gov_intel_counterfactual`, `fa_gov_intel_export_history`). 27 new routes under `/intelligence/`. Migration 0147 (replay-safe, RLS, 3 append-only tables with PG rules). CI gate `tools/ci/check_governance_provenance.py` (12 checks). **420+ tests** (8 files). All CI gates pass. | ✅ Complete | pr/18.5a-governance-intelligence-provenance |
| PR 18.5 | Governance Intelligence Authority | New bounded context `services/governance_intelligence/` (20 modules) — authority for governance explainability, simulation, policy lifecycle, benchmarking, forecasting, confidence scoring, external events, and federation. `api/db_models_governance_intelligence.py`: 10 tables — `fa_gov_intel_simulation`, `fa_gov_intel_simulation_history` (append-only), `fa_gov_intel_policy`, `fa_gov_intel_policy_version` (append-only), `fa_gov_intel_benchmark`, `fa_gov_intel_external_event` (append-only), `fa_gov_intel_federation`, `fa_gov_intel_explainability`, `fa_gov_intel_confidence_history` (append-only), `fa_gov_intel_timeline` (append-only). 47 routes under `/intelligence`. Policy lifecycle state machine: DRAFT→REVIEW→APPROVED→ACTIVE→DEPRECATED/SUPERSEDED→ARCHIVED (terminal). Simulation outputs always labeled PROJECTED with `is_production: false`. `anonymize_benchmark()` strips `tenant_id`. `build_governance_summary()` strips all tenant-identifying fields from federation summaries. Confidence scoring (data freshness, coverage, sample confidence → overall level). Linear regression for trend detection and forecasting. Pure-function modules (no I/O, no DB in domain logic). Scopes: `governance:read` / `governance:write`. RLS on all 10 tables via `current_setting('app.tenant_id', true)`. Migration 0146 (replay-safe; DO $$ policies + append-only rules on 5 timeline tables). CI gate `tools/ci/check_governance_intelligence.py` (12 checks). **768 tests** (9 files). All CI gates pass. | ✅ Complete | pr/18.5-governance-intelligence |
| PR 18.4 | Continuous Governance Orchestration Authority | New bounded context `services/governance_orchestration/` (22 modules) — autonomous governance coordinator that continuously evaluates organizational governance state, detects triggers (13 types), determines reassessment, orchestrates downstream authorities, and maintains a closed-loop governance lifecycle. `api/db_models_governance_orchestration.py`: 12 tables — `fa_gov_orch_policy`, `fa_gov_orch_policy_version` (append-only), `fa_gov_orch_playbook`, `fa_gov_orch_workflow`, `fa_gov_orch_reassessment`, `fa_gov_orch_trigger`, `fa_gov_orch_trigger_timeline` (append-only), `fa_gov_orch_simulation`, `fa_gov_orch_approval`, `fa_gov_orch_maintenance_window`, `fa_gov_orch_change_detection`, `fa_gov_orch_timeline` (append-only). 43 routes under `/governance-orchestration/`. Deterministic policy-as-code engine (pure functions, no DB). Trigger detection + policy-versioned recording. 7-state workflow FSM with valid-event graph. Reassessment lifecycle (REQUESTED → SCHEDULED → COMPLETED/FAILED/CANCELLED). Multi-stage approval chains with quorum + delegation. Maintenance-window trigger suppression. Change detection with deterministic impact classification (CRITICAL/HIGH/MEDIUM/LOW/NONE). Impact analysis with governance score deltas and control-effectiveness projections. 7 built-in playbook templates (PCI DSS, HIPAA, NIST CSF, ISO 27001, SOC 2, Microsoft Secure Score, CIS Controls). Continuous evaluation loop with graceful cross-authority degradation. New scopes: `governance:read` / `governance:write`. RLS on all 12 tables via `current_setting('app.tenant_id', true)`. Migration 0145 (replay-safe; DO $$ policies + append-only rules on 3 timeline tables). CI gate `tools/ci/check_governance_orchestration.py` (12+ checks). **800+ tests** (9 files). All CI gates pass. | ✅ Complete | pr/18.4-governance-orchestration |
| PR 18.3 | Enterprise Remediation Authority | New bounded context `services/remediation_authority/` (20 modules) — full remediation lifecycle from finding through verified closure: plan, task, assignment, dependency graph, verification, effectiveness measurement, SLA management, deterministic forecasting, risk burn-down, and continuous reassessment. 13-state machine (DRAFT → PLANNED → ASSIGNED → IN_PROGRESS → BLOCKED → AWAITING_EVIDENCE → UNDER_REVIEW → VERIFIED → COMPLETED → FAILED → REOPENED → CANCELLED → ARCHIVED). Circular dependency detection + critical path calculation. Evidence sufficiency engine. `api/db_models_remediation_authority.py`: 7 tables — `fa_rem_plan`, `fa_rem_task`, `fa_rem_timeline` (append-only, ORM + PG-rule guards), `fa_rem_assignment`, `fa_rem_dependency`, `fa_rem_verification`, `fa_rem_evidence_link`. 24 routes under `/remediation-authority/` in the evidence plane. New scopes: `remediation:read` / `remediation:write`. RLS on all 7 tables via `current_setting('app.tenant_id', true)`. Migration 0144 (replay-safe; DO $$ policies + append-only timeline rules). CI gate `tools/ci/check_remediation_authority.py` (12+ checks). Existing `services/remediation/` untouched. **1,640 tests** (8 files). All CI gates pass. | ✅ Complete | pr/18.3-remediation-authority |
| PR 18.2 | Enterprise Engagement Portal Authority | New bounded context `services/engagement_portal/` — read-through orchestration facade over Evidence, Reports, Remediation, Trust, Transparency, Timeline, Verification, and Notifications. No duplicated business logic — all crypto delegates to Trust Authority; cross-authority reads are wrapped in try/except for graceful degradation. `api/db_models_engagement_portal.py`: 3 portal-owned tables — `portal_engagement_preferences` (per-tenant), `portal_engagement_activity` (append-only, ORM event guards), `portal_engagement_notifications`. 15 routes under `/portal/engagement` in the control plane: `GET /health` (public), `GET /dashboard`, `GET /timeline`, `GET /evidence`, `GET /reports`, `GET /remediation`, `GET /trust`, `GET /transparency`, `GET /activity`, `GET /statistics`, `GET /search?q=`, `GET /notifications`, `GET /preferences`, `PUT /preferences`, `POST /activity`. New scopes: `portal:read` / `portal:write`. Tenant isolation enforced at route + DB layer with RLS policies. Migration 0143 (replay-safe; DO $$ policies + append-only rules). CI gate `tools/ci/check_engagement_portal.py` (12 checks). **640 tests** (7 files). All CI gates pass. | ✅ Complete | pr/18.2-engagement-portal |
| PR 16.5.1 | Control Effectiveness Explainability & Governance Action Engine | New bounded context `services/control_effectiveness_explainability/`. Transforms Control Effectiveness from a scoring system into a governance decision-support system. **Score Contribution Engine**: per-component weighted breakdown (7 components), impact classification (POSITIVE ≥70 / NEGATIVE <50 / NEUTRAL), contribution_percentage summing to 100%. **Root Cause Analysis**: rules-based signal generation — 16 signal types (8 negative, 8 positive) with severity, impact_score, and description; negatives sorted before positives by impact magnitude. **Governance Action Engine**: deterministic rule-based recommendations — 8 action types (REVIEW_VERIFICATION_WORKFLOW, REFRESH_EVIDENCE, COLLECT_ADDITIONAL_EVIDENCE, RESOLVE_ACTIVE_EXCEPTIONS, REVIEW_GOVERNANCE_HEALTH, INVESTIGATE_DECLINING_TREND, ADDRESS_FORECAST_RISK, MONITOR_TREND); actions sorted by priority (CRITICAL → HIGH → MEDIUM → LOW). **Control Health Narrative**: template-driven deterministic summaries — no AI, no LLMs; auditable; mentions level, trend, top 2 positive/negative factors, top recommended action. **Change Detection**: 5 statuses (IMPROVED >10 / IMPROVING >3 / STABLE / DECLINING <-3 / CRITICAL <-10) from 7d/30d/90d deltas. **Governance Priority**: 4-level classification (CRITICAL/HIGH/MEDIUM/LOW) from effectiveness score + trend + forecast + exception signals. **Rankings Engine**: 7 rank types (TOP/WEAKEST/FASTEST_IMPROVING/FASTEST_DECLINING/HIGHEST_RISK/MOST_FRAGILE/MOST_VALUABLE) stored in `fa_control_ranking`; refreshed by `recalculate_all()`. **Executive Dashboard**: extended dashboard with priority distribution, top positive/negative signals, top recommended actions, on-the-fly or stored rankings. **CGIN Foundation**: 4 benchmark-ready snapshot schemas (ControlContributionSnapshot, ControlRiskSnapshot, ControlActionSnapshot, ControlPrioritySnapshot) — structures only, no benchmarking yet. 1 new DB table (`fa_control_ranking`), migration 0134. 6 read-only routes: `GET /executive-dashboard`, `GET /priorities`, `GET /rankings`, `GET /explain/{control_id}`, `GET /contributors/{control_id}`, `GET /actions/{control_id}`. Route ordering preserved: explainability router registered before CE router. 3 Prometheus counters. **246 tests** (CEX-1–CEX-246). All CI gates pass. | ✅ Complete | #TBD |
| PR 16.5 | Control Effectiveness Engine | New bounded context `services/control_effectiveness/`. Deterministic 7-component scoring engine: Coverage 20%, Verification 20%, Freshness 15%, Trend 15%, Forecast 10%, Evidence Density 10%, Exception 10%. All scores 0–100, weighted sum clamped and rounded. 5 effectiveness levels (HIGHLY_EFFECTIVE/EFFECTIVE/ADEQUATE/WEAK/INEFFECTIVE) and 4 risk classifications (LOW/MEDIUM/HIGH/CRITICAL). 2 new DB tables: `fa_control_effectiveness` (mutable current state, delete-protected at ORM+PG layer) and `fa_control_effectiveness_history` (append-only at both layers). 6 routes under `/control-effectiveness`: `GET /dashboard`, `GET /cgin/snapshot`, `POST /recalculate`, `GET /`, `GET /{control_id}`, `GET /history/{control_id}`. Trend data uses 2× window constraint from PR 14.6.9. `CONTROL_EFFECTIVENESS` added to `SourceType` enum + `TIMELINE_ADAPTERS`. 5 Prometheus counters. Route prefix registered in `control` plane. Migration 0133. All CI gates pass. | ✅ Complete | #TBD |
| PR 14.6.9 | Trend Persistence & Governance Forecasting | Extended `services/freshness_score_history/` with trend persistence and linear governance forecasting. `run_snapshot()` now persists 3 `FaFreshnessTrendSnapshot` rows (7d/30d/90d) per snapshot run: `average_score`, `score_delta`, `fresh_delta`, `expired_delta`, `coverage_risk_delta` vs. period baseline. `get_forecast()`: velocity = `(current - baseline_30d) / 30`; projections = `clamp(0, 100, current + velocity * horizon)` for 30/60/90d; early warning when `velocity < 0 and (current - threshold) / abs(velocity) <= horizon_days`. 2 new routes: `GET /freshness/trends/history` (paginated trend snapshot history), `GET /freshness/forecast` (governance projection with early warning flag). No new tables or migrations — `fa_freshness_trend_snapshots` already existed from PR 14.6.8 migration 0132. All CI gates pass. | ✅ Complete | #TBD |
| PR 14.6.8 | Freshness Score History & Governance Trend Intelligence | New bounded context `services/freshness_score_history/`. Append-only per-evidence daily snapshots (`fa_freshness_score_snapshots`), tenant-wide daily aggregations (`fa_freshness_daily_snapshots`). Deterministic trend arithmetic over 7d/30d/90d/180d/365d windows. Governance decay detection: IMPROVING (delta > +5) / STABLE (-5 ≤ delta ≤ +5) / DEGRADING (delta < -5) / CRITICAL (delta < -15). Idempotent `POST /freshness/snapshots/run` (one snapshot per evidence per day per tenant). `GET /freshness/history/{evidence_id}` (per-evidence score timeline). `GET /freshness/trends` + `/freshness/trends/dashboard` (7d/30d/90d deltas, freshness_velocity, coverage_velocity, risk_velocity). `GET /freshness/cgin/trends` (CGIN benchmark-ready dataset). Timeline integration (4 event types: snapshot.created, trend.generated, decay.detected, improvement.detected). 5 Prometheus counters. 89 tests, 19 classes. CGIN foundation for Control Effectiveness (16.5) + Executive Intelligence (19.x). All CI gates pass. | ✅ Complete | #TBD |
| — | PR 14.6.5A: Evidence Status Model Hardening & Governance Completion | ✅ done 2026-06-23 | Hardening and completion pass on `services/evidence_authority/`. **Canonical Verification Authority** — `fa_verifications` table (append-only; ORM + PG trigger guards): 7 verification types (MANUAL_REVIEW/DOCUMENT_REVIEW/TECHNICAL_VALIDATION/CONTROL_TEST/AUTOMATED_VALIDATION/THIRD_PARTY_ATTESTATION/AI_ASSISTED_REVIEW), 6 actor types (HUMAN/REVIEWER/APPROVER/AI_SYSTEM/AUTONOMOUS_AGENT/AGI_SYSTEM), 3 results (PASS/FAIL/INCONCLUSIVE). Verification records survive evidence lifecycle changes. **Verification History** — `GET /{ev_id}/verifications` (list) + `GET /{ev_id}/verifications/summary` (verification_count, passed_count, verification_success_rate, verification_age_days, latest_verification_at/type). **Verification SLA Model** — `review_due_at`, `verification_due_at`, `freshness_due_at` nullable columns on `fa_evidence`; `PUT /{ev_id}/sla` to set; `GET /{ev_id}/sla` returns deterministic ON_TRACK/DUE_SOON/OVERDUE per deadline (DUE_SOON = within 7d). **Control & Risk Linkage** — `fa_evidence_control_links` (many-to-many evidence↔control; unique per pair) + `fa_evidence_risk_links` (evidence↔RISK/FINDING/EXCEPTION; `link_type` discriminator; unique per triple). Both append-only with ORM + PG trigger guards. **Coverage Analytics** — `GET /evidence/coverage`: controls_with_evidence, controls_without_evidence (cross-ref `control_registry`), risks_with_evidence (cross-ref `risk_acceptances`), verified_controls, evidence_density, coverage_percentage, total_known_controls. **Governance Health Signals** — `GET /evidence/health`: verification_overdue_count, review_overdue_count, freshness_overdue_count, orphaned_evidence_count, unlinked_evidence_count, disputed/invalidated/attested/verified evidence counts. **Benchmark Placeholders** — 4 nullable INTEGER columns on `fa_evidence` (benchmark_freshness/verification/density/coverage_percentile) for future CGIN industry benchmarking; no computation yet. **CGIN Snapshot Bundle** — `GET /evidence/cgin/snapshot`: deterministic `CGINSnapshotBundle` (bundle_id, bundle_version=1.0) containing `EvidenceStatusSnapshot[]`, `VerificationSnapshot[]`, `CoverageSnapshot`, `HealthSnapshot`; versioned, anonymizable, replayable. **Timeline events** — 9 new types: verification_created, verification_failed, evidence_linked_to_control, evidence_linked_to_risk, evidence_linked_to_finding, evidence_linked_to_exception (+ SLA and coverage events). **7 new Prometheus counters**: evidence_verifications_total, evidence_verification_failures_total, evidence_verification_overdue_total, evidence_control_links_total, evidence_risk_links_total, evidence_coverage_calculations_total, evidence_health_updates_total. **13 new routes** (3 tenant-level before `/{ev_id}`: /coverage, /health, /cgin/snapshot; 10 per-evidence: verifications CRUD, SLA, control-links, risk-links). Migration `0129_evidence_hardening.sql`. **116 tests** (all CI gates pass). |
| — | PR 14.6.5: Canonical Evidence Status Model | ✅ done 2026-06-23 | Extended `services/evidence_authority/` with: **ATTESTED trust state** (floor=45, sits between PARTIALLY_VERIFIED/25 and VERIFIED/60; bidirectional transitions in all trust FSM maps). **BUSINESS_OWNER + TECHNICAL_OWNER ownership roles** (additive enum values; no migration). **Quality scoring engine** `services/evidence_authority/quality.py` — three pure deterministic score functions, zero I/O: `compute_freshness_score` (linear decay 100→0 over 365d from collected_at; proportional when expires_at present; REVOKED/EXPIRED/ARCHIVED → 0), `compute_verification_score` (base=TRUST_STATE_SCORE_FLOOR + min(count×5, 15) + 5 if source), `compute_completeness_score` (6 field weights summing to 100: description+20, owner_id+25, source_system+15, expires_at+20, engagement_id+10, source_ref+10). **Quality score persistence**: `_persist_quality_scores(row)` called inside every mutating operation (create/transition/verify) before commit; also via explicit `POST /{ev_id}/quality/compute` endpoint. **4 nullable columns** added to `fa_evidence`: `freshness_score`, `verification_score`, `completeness_score`, `quality_last_computed_at`. **`evidence_status_changed` canonical timeline event** — emitted on every lifecycle AND trust state transition; `change_type` field distinguishes the two. **Governance reporting endpoint** `GET /evidence/status/report` (audit:read): per-item status + lifecycle/trust aggregations + avg quality scores + health indicators (`without_owner_count`, `expired_count`, `expiring_soon_count`, `disputed_count`, `invalidated_count`, `attested_count`). **3 new Prometheus counters**: `frostgate_evidence_status_transitions_total{to_status}`, `frostgate_evidence_trust_changes_total`, `frostgate_evidence_quality_score_updates_total`. Route ordering: `/evidence/status/report` and `/{ev_id}/quality/compute` before `/{ev_id}` catch-all. Migration `0128_evidence_status_model.sql`: 4 new columns + 2 partial indexes. **122 tests** (all CI gates pass). |
| — | PR 14.6.2: Timeline Authority (Canonical Governance Ledger) | ✅ done 2026-06-22 | New bounded context: services/timeline_authority/, fa_timeline_events table (migration 0125), 8 routes under /timeline-authority/*, deterministic IDs, hash chain, replay engine, export, integrity verification, 6 Prometheus counters, 110+ tests. All CI gates pass. |
| — | PR 14.6.1: Canonical Evidence Authority | ✅ done 2026-06-22 | New bounded context `services/evidence_authority/`. Foundational platform capability: `fa_evidence` is the single canonical source of truth for all evidence in FrostGate — no other subsystem creates competing ownership. 5 ORM tables (all append-only where appropriate): `fa_evidence` (canonical entity), `fa_evidence_ownership` (append-only role history; ORM + PG trigger guards), `fa_evidence_relationships` (append-only M2M links to governed resources; ORM + PG trigger guards), `fa_evidence_trust_events` (append-only, hash-chained trust transitions; SHA-256 chain with `prev_event_hash` for regulator-verifiable integrity), `fa_evidence_audit_events` (append-only lifecycle audit trail; H13-pattern `transaction_id`/`correlation_id`). Migration `0123_canonical_evidence_authority.sql`. `EvidenceAuthorityEngine` is the single write authority: `create_evidence` (integrity_hash on identity fields), `transition_lifecycle` (10-state FSM: DRAFT/COLLECTED/SUBMITTED/UNDER_REVIEW/VERIFIED/REJECTED/SUPERSEDED/EXPIRED/REVOKED/ARCHIVED; temporal markers auto-set), `verify_evidence` (6-state trust FSM: UNVERIFIED/PARTIALLY_VERIFIED/VERIFIED/HIGH_CONFIDENCE/DISPUTED/INVALIDATED; TRUST_STATE_SCORE_FLOOR floors enforced; hash-chained events), `assign_ownership`/`revoke_ownership` (5 roles: OWNER/REVIEWER/VERIFIER/APPROVER/CUSTODIAN; AGI-forward `actor_type`: human/service/agent/autonomous_system), `link_relationship` (9 entity types, 5 relationship types; conflict guard; reverse lookup), `dashboard` (12 aggregated counters). 15 routes under `/evidence`: `GET /dashboard`, `GET /by-entity/{type}/{id}`, `POST /evidence`, `GET /evidence`, `GET /{ev_id}`, `PATCH /{ev_id}`, `POST /{ev_id}/lifecycle`, `POST /{ev_id}/ownership`, `DELETE /{ev_id}/ownership/{id}`, `GET /{ev_id}/ownership`, `POST /{ev_id}/verify`, `GET /{ev_id}/trust`, `POST /{ev_id}/relationships`, `GET /{ev_id}/relationships`, `GET /{ev_id}/audit`. Route ordering: dashboard + by-entity before `/{ev_id}`. 6 new Prometheus counters (`evidence_created/verified/rejected/superseded/expired/archived_total`). Immutability: ORM `before_update`/`before_delete` guards + PostgreSQL triggers (defense in depth). Forward-compatibility fields for FA-17.5: `content_hash`, `integrity_hash`, `provenance_chain_head`. **110 tests** (EA-1–EA-110). All CI gates pass. |
| — | PR 14.4: Governance Portal Integration & Client Trust Layer | ✅ done 2026-06-22 | New bounded context `services/governance_portal/` — read-through façade over `risk_acceptance`, `risk_governance`, and `control_registry` data; portal engine never writes to those tables. 2 portal-owned append-only ORM models: `portal_acknowledgements`, `governance_portal_audits`. `GovernancePortalEngine`: 11 tenant-scoped methods (`dashboard`, `list_risks`, `get_risk`, `list_controls`, `get_control`, `list_evidence`, `get_evidence`, `create_acknowledgement`, `list_acknowledgements`, `get_acknowledgement`, `get_audit`). Evidence freshness states: FRESH (elapsed < 50% of `review_frequency_days`), AGING (50–99%), EXPIRING_SOON (remaining ≤ 30 days), EXPIRED (overdue or never verified). Governance health score (0–100): 40% verified control ratio + 30% fresh evidence ratio + 30% non-active risk ratio. 11 routes under `/portal/governance/`: `GET /dashboard`, `GET /risks`, `GET /risks/{risk_id}`, `GET /controls`, `GET /controls/{ctl_id}`, `GET /evidence`, `GET /evidence/{evidence_id}`, `POST /acknowledgements`, `GET /acknowledgements`, `GET /acknowledgements/{ack_id}`, `GET /audit`. All require `governance:read` or `governance:write` scope. `GOVERNANCE_PORTAL` added to `SourceType` enum + `TIMELINE_ADAPTERS`. `PORTAL_ACK_CREATED` added to `NotificationTrigger`. 6 new Prometheus counters (bounded cardinality). `/portal/governance` prefix registered in control plane. Contracts + route inventory regenerated. **106 tests** (101 GP-1–GP-101 + 5 adapter completeness). All CI gates pass. |
| — | PR 1.10: Trust Replay Engine | 🗓 planned | Six components: **Replay Engine Core** (point-in-time query layer over append-only records; `replay_trust_state(tenant_id, engagement_id, at)` reconstructs complete trust state at timestamp T); **Trust State Replay** (evidence collected, chains intact, records signed, authority in force, report links live — all as they existed at T); **Trust State Diff** (`diff_trust_state(from_t, to_t)` — delta between two replay points; answers "what changed between assessment date and report delivery?"); **Replay Manifest** (deterministic manifest of all nodes included in the replay, each with its `event_hash` and `signed_at`; manifest itself is hashed and signable); **Trust Proof Generation** (cryptographic proof object — self-proving because every node's `event_hash` was written before the question was asked; no mutable state in the reconstruction); **Auditor / Regulator Proof Package** (exportable package: replay manifest + proof object + chain of custody + report signatures; answers "what did you know on date X and how do you prove it?" with a verifiable artifact, not logs) |

## How to add a PR to this roadmap

When a PR merges:
1. Add a row to the relevant Phase table with: PR number, title, status ✅, one-line deliverable
2. If it closes a P0/P1 item in Phase 2, update that row's Status and PR columns
3. If it introduces something not on the plan, add it as a new row — do not leave it untracked

---

| — | PR #513 — Phase 1: Capability Registry CI Sprawl Guard | ✅ merged (#513) 2026-07-07 | `CAPABILITY_REGISTRY` in `api/actor_context.py` extended to 6 read + all existing write/governance capabilities. CI sprawl guard (`ALL_PERMISSIONS` and `ROLE_PERMISSIONS` must stay in sync with `CAPABILITY_REGISTRY`). Unit test enforces sync at every CI run. |
| — | PR #514 — Phase 2: FA Mutation Route Hardening | ✅ merged (#514) 2026-07-07 | All 40 mutation routes in `api/field_assessment.py` hardened with `require_permission("X.Y")` via `ActorContext`. `require_scopes` calls replaced with `authz_scope` (metadata-only). Regenerated OpenAPI contracts. |
| — | PR #516 — Phase 2: Post-Migration Fixes | ✅ merged (#516) 2026-07-07 | Backward-compat scope fallback (`governance:write`→assessor, `governance:qa_approve`→qa_reviewer, `governance:read`→viewer); 3 portal-grant routes changed from `tenant.configure`→`assessment.create`; `patch_finding_status_route` changed from `finding.create`→`finding.close`; all test fixtures repaired (89 failures → 0). |
| — | PR #517 — Phase 3: FA GET Route Read-Capability Enforcement | ✅ merged (#517) 2026-07-08 | All 43 FA GET route handlers enforced with `require_permission("X.read")`. `emit_alerts` write gate (viewer cannot trigger alerts via GET). Global key (`FG_API_KEY`) fix in `api_key.py` — returns `platform_admin` `ActorContext`. 29 denial/smoke tests. |
| — | PR #518 — Phase 4: Governance Intelligence + Orchestration Enforcement | ✅ merged (#518) 2026-07-08 | New `governance.read` capability added to all 5 non-platform roles. 73/74 routes in `governance_intelligence.py` and 34/35 routes in `governance_orchestration.py` enforced (health exempt). 22 new tests. |
| — | PR #519 — Phase 5: P0/P1 Governance + Admin Enforcement | 🔄 open (feat/phase5-p0p1-governance-admin-enforcement) | 83 injections across 7 files: `decisions.py`, `governance.py`, `risk_acceptance.py`, `risk_governance.py` (P0); `keys.py`, `admin.py`, `admin_identity.py` (P1). Legacy scope fallback extended to cover `admin:write/read`, `keys:admin/write/read` → `platform_admin`. 16 new tests. |
| — | PR #539 — TC-0: Assert MINISIGN_SECRET_KEY at startup | ✅ merged (#539) 2026-07-14 | G1.1 cleared. `MINISIGN_SECRET_KEY` now raises `RuntimeError` at startup in production (severity="error"). `FG_BILLING_EVIDENCE_HMAC_KEY` assertion pre-existing. 3 tests. |
| — | TC-7: Wire report quality scores to real evidence sources | ✅ merged (#540) 2026-07-14 | G1.3 cleared. Replaces `_PLACEHOLDER_COVERAGE = 0.5` in `engine.py` with live DB reads. New `services/report_authority/quality_inputs.py` — 5 metrics from `fa_evidence_links`, `fa_evidence_provenance`, `fa_normalized_findings`. 15 tests (includes Codex P2 fixes: evidence_coverage scoped to finding/normalized_finding links; verification_coverage counts chain head records). |
| — | FA-1: Portal page validation with live engagement data | ✅ merged (#541) 2026-07-14 | G1.5 cleared. Fixes `asset_name` broken binding in attestation page (`AssetResponse.asset_name` populated from `name`). Adds `last_attested_at`, `next_attestation_due`, `owner_email` to `GET /governance/assets` via batch `GaAssetOwner` lookup (most-overdue owner wins via `ORDER BY next_attestation_due_at ASC NULLS FIRST`). 6 acceptance tests (FA1-1 through FA1-2b). |
| — | P-1: Provisioning to portal activation is deterministic | ✅ merged (#542) 2026-07-14 | G1.8 cleared. Extends `CompleteWorkflowRequest` with optional `client_id` and `engagement_id`. When both provided, `complete_workflow` auto-creates a `portal_grant` within the same transaction and returns `raw_secret` (shown once). Client can authenticate at `POST /portal/authenticate` with the returned secret — no manual operator step. 4 integration tests (P1-1 through P1-3, P1-4). |
| — | R-1: Remediation authority declared canonical | ✅ merged (#543) 2026-07-15 | G1.4 cleared. Declares `api/remediation_authority.py` (routes at `/remediation-authority/*`) as the canonical remediation system. All 17 routes in `api/remediation.py` (legacy `/remediation/*`) marked `deprecated=True` in OpenAPI. Module docstring updated with NON-CANONICAL header. `api/portal_remediation.py` docstring clarified: routes through `PortalRemediationEngine`, not the legacy router. 3 acceptance tests (R1-1 through R1-3). |
| — | R-2: Portal remediation validated with live data | ✅ merged (#545) 2026-07-15 | G1.9 cleared. Wires `RemediationCenter.tsx` 4-tab view (open/overdue/completed/blocked) into the portal home dashboard. Fixes Codex P2: completed/blocked tabs fetch terminal findings via `listFindings` in parallel — not from roadmap which only returns open/in_progress. 12 acceptance tests (R2-1 through R2-4). |
| — | FA-2: Connector credential pre-flight validation | ✅ merged (#546) 2026-07-15 | G1.6 cleared. Adds `validate_msgraph_tenant_preflight()` in `services/connectors/msgraph/preflight.py`: UUID format check + OIDC discovery probe against Azure AD before device-code flow. Hooked into `POST /engagements/{id}/connector-runs/msgraph/initiate`. Invalid UUID → 422 CONNECTOR_INVALID_TENANT_FORMAT; unknown tenant → 422 CONNECTOR_TENANT_NOT_FOUND; network error → 502. 8 acceptance tests (FA2-1 through FA2-8). |
| — | P-2: Auto-enable AI assistant on QA approval | ✅ merged (d7548c17) 2026-07-16 | G1.7 cleared. Sets `portal_ai_enabled = True` in `FaEngagement.engagement_metadata` on QA approval in `api/field_assessment.py`. Portal retains aiEnabled gate to guard pre-P2 grants and non-QA-approved sessions. 8 acceptance tests (P2-1 through P2-8). |
| — | P-3: Zero-touch client provisioning via Edge Config | ✅ merged 2026-07-16 | Admin creates client in console → portal is live immediately, no manual env var steps. Adds `@vercel/edge-config` to portal. New `apps/portal/lib/tenant-registry.ts`: reads `tenants` key from Edge Config (30 s in-process cache). Updates portal BFF `resolveAuth()`: env-var allowlist → Edge Config fallback → static CORE_TENANT_ID. Removes early `!CORE_API_KEY` guard (key may come from Edge Config). One-time Vercel setup required: connect Edge Config store to both console and portal projects; set `VERCEL_API_TOKEN` on console project for write access. |
| — | P-3a: Redis fallback for zero-touch provisioning | ✅ done 2026-07-17 | Eliminates the "manual step required" warning when Edge Config is absent. Console `provision-tenant` route writes `portal:tenant:{id}:key` to Redis (`EX 31536000`) as fallback after Edge Config attempt. Portal `tenant-registry.ts` reads same key via shared `REDIS_URL`. `lib/redis.ts` now checks `PORTAL_REDIS_URL \|\| REDIS_URL`. Console reads `BFF_REDIS_URL \|\| REDIS_URL`. Raw API key suppressed from provisioning response when any registry backend (Edge Config or Redis) succeeds. Docker-compose Redis exposed on `127.0.0.1:6379` for local dev. Portal `/api/health` reports Redis connectivity status (`ok/degraded/unavailable/not_configured`). 20 tests in `portal-tenant-registry.test.js` (11 static, 9 behavioral); 5 new tests in `tenant-provisioning-policy.test.js`. |
| — | FIX: 403 on portal grants / admin identity / workforce user invite | ✅ done 2026-07-17 | Three auth failures during user invite flow: (1) `portal/grants` — removed `portal.remediation` capability gate and changed scope from `governance:write` → `admin:read/write`; (2) `admin/identity/tenants/*` — console BFF was sending tenant API key instead of admin gateway token, causing `bind_tenant_id` mismatch; (3) `workforce/users` GET — downgraded from `admin:write` → `admin:read`, removed `identity.scim` capability gate on POST. Console BFF now detects cross-tenant admin paths (`isCrossTenantAdminPath`) and injects `FG_ADMIN_GATEWAY_TOKEN` headers (`X-API-Key`, `X-FG-Internal-Token`, `X-Admin-Gateway-Internal: true`) instead of the tenant API key. Admin paths bypass `X-Tenant-ID` injection and preserve original query params including `tenant_id`. |
| — | PR A+B: Trusted tenant resolution + metadata-only registry (#548) | ✅ merged (#548) 2026-07-17 | **PR A — Trusted tenant resolution**: Console BFF `resolveAuthorizedTenant()` validates `?tenant_id=` URL params against the authenticated session before delegating. `internal_console`/`legacy_internal` users may act on any tenant; `console_enabled_client` users may only act on their own session tenant; all others → 403; malformed tenant IDs → 422. Removes cross-tenant access bug where any authenticated user could switch tenant context via URL param. `workforce/users` removed from `isCrossTenantAdminPath` — now uses tenant-scoped API key resolution. **PR B — Metadata-only registry**: `TenantRecord.api_key` made optional/deprecated; console tenant registry (`console:tenant-registry` in Upstash) now stores display metadata only (label, created_at). `upsertTenantInRegistry` and `upsertTenantInUpstash` accept `Omit<TenantRecord, 'api_key'>`. Portal key writes (`writeKeyToRedis`, `writeKeyToUpstash` → `portal:tenant:{id}:key`) always run independent of Edge Config success. `getTenantApiKey` reads portal key first, falls back to legacy registry `api_key` field for pre-Phase-4 tenants. 28 new/updated tests across 3 test files. |

| — | Platform Recovery R0: Fail-closed provisioning hotfix | ✅ merged (#549) 2026-07-18 | `provision-tenant` route now fails closed when both Redis and Upstash writes fail: revokes the dangling Postgres key and returns `503 PERSISTENCE_UNAVAILABLE` instead of exposing the raw API key. `revokeKey()` helper added. Raw key never appears in any response. Tests: `'provision tenant never exposes raw API key'`, `'provision tenant fails closed with 503'`. |
| — | Platform Recovery R1: Authority and persistence audit | ✅ merged (#549) 2026-07-18 | `docs/ai/R1_AUTHORITY_AUDIT.md` — full audit of all tenant creation paths, credential paths, internal gateway tokens, persistence locations, duplicate system matrix, and migration risk register. Critical finding: no `tenants` table in Postgres; identity lived in `state/tenants.json`. Confirmed `api/credentials.py` is dead code. |
| — | Platform Recovery R7: Canonical tenant persistence — Postgres-authoritative tenant registry | ✅ merged (#549) 2026-07-18 | Migration `0156_canonical_tenants.sql`: new `tenants` table (12 cols, no RLS — platform identity PK, not per-tenant scope FK) + `tenant_migration_ledger`. `api/tenant_repository.py`: `TenantRepository` with Postgres-first CRUD and JSON fallback during transition. `tools/tenants/migrate_to_postgres.py`: 11-step idempotent migration with freeze sentinel (`tenants.json.frozen`). `tools/tenants/registry.py`: freeze guard centralized in `_save_raw()` — covers all write paths. `api/admin.py`: `create/list/get_tenant` routes Postgres-first with `IntegrityError` → 409 handling. `tools/ci/check_core_rls.py`: `_PLATFORM_IDENTITY_TABLES` exemption set. 16 tests including critical invariant: tenant resolves from Postgres when JSON is absent. **R7 does NOT own:** lifecycle transitions (activate/suspend/archive/delete), API key rotation on the tenant record, or suspension persistence — those are R3's authority. Current `suspend/activate` admin routes write to `tenant_usage` tracker only; `tenants.lifecycle_state` is not updated until R3 lands. |
| — | Platform Recovery R6: Gateway secret convergence — Deploy 1 (repository-side unification) | ✅ merged (#553) 2026-07-18 | `api/config/internal_gateway_secret.py`: single canonical Python resolver — `FG_INTERNAL_GATEWAY_SECRET → FG_ADMIN_GATEWAY_INTERNAL_TOKEN → FG_INTERNAL_AUTH_SECRET → FG_INTERNAL_TOKEN`. `apps/console/lib/internal-gateway-secret.ts`: TypeScript equivalent — `FG_INTERNAL_GATEWAY_SECRET → FG_ADMIN_GATEWAY_TOKEN → FG_INTERNAL_AUTH_SECRET → FG_INTERNAL_TOKEN`. All three Python guards (`require_internal_admin_gateway`, `_admin_gateway_internal_token`, `_internal_guard`) and both TypeScript console routes consume shared resolvers; previously divergent chains eliminated. 71 tests. CI dummy envs updated; SOC-HIGH-002 gate satisfied. **Deploy 2 blocked on infrastructure rotation**: `FG_INTERNAL_GATEWAY_SECRET` must be configured and verified across every runtime environment before `required_env.py` enforcement is added. Merge: `99e5e95d`. Inventory follow-up: `819ff965`. |
| — | Platform Recovery R3: Tenant lifecycle authority | ✅ merged (#554) 2026-07-19 | `api/tenant_lifecycle.py`: single authority for all tenant state transitions — `execute_transition()` is the only path that writes `lifecycle_state`. State machine: `active → suspended\|archived`, `suspended → active\|archived`, `archived → deleted` (terminal). Atomic: validate → idempotency check → conditional `UPDATE WHERE lifecycle_state = :from_state` (rowcount=0 raises on concurrency) → audit INSERT. Idempotency scoped to `(tenant_id, idempotency_key)`. `archived_at` immutable via `WHERE archived_at IS NULL`. New admin routes: archive, delete, lifecycle-history. `tenant_repository.py`: `deleted` added; `list_all()` excludes both archived and deleted. Migration `0157_tenant_lifecycle_transitions.sql`. 25 tests. |
| — | Platform Recovery R3b: Lifecycle enhancements | ✅ merged (#555) 2026-07-19 | `transition_hash`: SHA-256 of `(transition_id, tenant_id, from_state, to_state, occurred_at, request_id, actor_id)` — tamper-evident fingerprint per audit record. `schema_version`: `DEFAULT 0` for pre-0158 rows; `TRANSITION_SCHEMA_VERSION = 1` set explicitly by `execute_transition()`. Terminal-state regression tests (deleted → * always raises). `archived_at` immutability confirmed by test. `get_transition_history()` explicit `ORDER BY occurred_at DESC`. Migration `0158_tenant_lifecycle_transitions_v2.sql`. 41 tests total (10 test classes). |
| — | Platform Recovery R4: Credential authority | 🔜 in progress (R4.6 next) | Single canonical authority for tenant credential lifecycle. Initial scope: `tenant_api_key` only. Deferred: portal_access, connector, agent/device, service_identity, internal gateway (R6). **R4.1** ✅ — Credential inventory and authority contract: `docs/security/CREDENTIAL_AUTHORITY.md`, `artifacts/security/credential_inventory.det.json`, `docs/security/CREDENTIAL_MIGRATION_DECISIONS.md`. Discovery complete; critical findings: duplicate admin rotation route (no tenant enforcement), `rotated_from` incoherent across paths, `key_lookup` = lookup_fingerprint (migration free). **R4.2** ✅ merged (#556) — Canonical credential persistence (`migrations/postgres/0159_tenant_credentials.sql`): `credential_slot`, `generation`, `lookup_fingerprint`, `lookup_key_version`, `secret_prefix`, `secret_hash`, `hash_algorithm`, `status` (pending\|active\|rotated\|revoked\|expired), `expires_at`, `last_used_at`, `approximate_use_count` (best-effort). `credential_slots` parent table for slot-level rotation serialization. **R4.3** ✅ merged (#556) — `api/credential_authority.py`: `issue_credential`, `validate_credential`, `rotate_credential`, `revoke_credential`, `expire_credentials`, `get_credential`, `list_credentials`, `get_credential_history`. Slot-level `FOR UPDATE` locking; `rowcount=0` rotation concurrency guard. `CredentialPrincipal` return type. Plaintext returned once, never stored. Occupied-slot guard (second issue raises). Terminal-generation rotation guard (revoked/expired cannot be rotated). 73 tests. CI gate `check_credential_authority` wired into fg-fast. **R4.4** ✅ merged (#557) — Lifecycle enforcement integration: `_get_tenant_lifecycle_state(conn)` via `TenantRepository.get_lifecycle_state_on_conn()` in `issue_credential` + `rotate_credential` (single pool slot; no second checkout); `validate_credential` retains JOIN (atomic, AUTHORIZED-DIRECT-TENANT-SQL); CI gate extended with TenantRepository positive assertion; 16 cross-authority integration tests (`test_r4_lifecycle_integration.py`). **R4.5** ✅ merged (#558 / 540b5849) — Audit events: `migrations/postgres/0160_tenant_credential_events.sql` (`tenant_credential_events` table, RLS, append-only trigger, 3 indexes); `issued`/`rotated`/`revoked`/`expired` events atomic within write transactions; `validated`/`validation_failed`/`denied_tenant_state` best-effort (never blocks hot path); `list_credential_events()` read function; `CredentialEventRecord` dataclass; CI gate extended with `tenant_credential_events` write-authority protection (SOC-HIGH-003); 22 tests. **R4.6** — Admin API and permissions: `credentials.read/issue/rotate/revoke`; routes under `/admin/tenants/{id}/credentials`. Fixes duplicate rotation route bug. **R4.7** — Dual-validation migration (Deploy 1): canonical preferred, legacy fallback, migration telemetry. **R4.8** — Legacy retirement and CI authority gate (Deploy 2+3): removes fallback, retires `api/credentials.py`, `api/key_rotation.py` (KeyRotationManager), `api/db/api_keys_store.py`. **R4.9** — Deferred credential classes (portal_access, connector, agent/device — if approved). |

*Last updated: 2026-07-20 (R3 merged #554, R3b merged #555, R4.2+R4.3 merged #556, R4.4 merged #557, R4.5 merged #558. R6 Deploy 2 blocked on infra rotation. Next: R4.6 admin API. Remaining: R4.6–R4.9, R2 retire duplicate authorities, R5 idempotent bootstrap, R8 provisioning CI gates, R9 provisioning ops console.)*


