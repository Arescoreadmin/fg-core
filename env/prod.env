# Production environment overrides
#
# This file is intentionally sparse. All secrets are injected via:
#   - CI: .github/actions/fg-secrets (POSTGRES_PASSWORD, REDIS_PASSWORD, NATS_AUTH_TOKEN, etc.)
#   - Local dev: .env (copy from .env.example)
#
# Add prod-specific NON-SECRET static config here when needed.
# Do NOT commit secrets to this file.
