# T6 Operational Rehearsal — Run Log

**Run ID:** T6-EXEC-20260804-001
**Date:** 2026-08-04
**Operator:** jcosat
**Result:** FAIL — second run required (6/18 steps failed; threshold >2)
**Duration:** ~4 hours (13:07:53Z – 17:10:00Z UTC)

---

## Summary

T6 is the first full H1–H18 operational rehearsal against the production platform.
Disposable tenant: `fg-t6-rehearsal-20260804-001`.

12 of 18 steps passed. 6 failed — all attributable to 3 root causes:

| Root cause | Steps blocked | Severity |
|---|---|---|
| D-T6-003: `ObjectDeletedError` post-commit in scan initiation routes | H5, H10, H11, H17 | High |
| D-T6-005: pre-commit 500 in `capture_observation_route` | H13 | High |
| D-T6-006: expired ORM attr in `qa_approve_report_route` response | H15 | Medium |

D-T6-002 (post-commit ORM expiry, same root cause as D-T6-003) affected H4, H12, H14 but data committed successfully; GET fallbacks recovered all three — recorded as PASS with defect.

D-T6-004 (portal invitation email missing `?tenant_id` param) blocked H8 twice. Workaround: navigate directly with `?tenant_id=fg-t6-rehearsal-20260804-001` after clearing portal cookies. H8/H9 passed on third attempt.

---

## Step Results

| Step | Result | Defect |
|---|---|---|
| H1 Migration status | PASS | D-T6-001 (stale suppress flag, non-blocking) |
| H2 Pre-engagement backup | PASS | — |
| H3 Tenant provisioning | PASS | — |
| H4 Engagement creation | PASS | D-T6-002 (500 + GET fallback) |
| H5 No-auth scans | FAIL | D-T6-003 |
| H6 Portal invitation | PASS | — |
| H7 Email delivery | PASS | — |
| H8 Invitation acceptance | PASS | D-T6-004 (3 attempts; cookie-clear workaround) |
| H9 Client portal access | PASS | — |
| H10 Device-code scan setup | FAIL | D-T6-003 + no MS tenant |
| H11 Full scan suite | FAIL | D-T6-003 + no MS tenant |
| H12 Questionnaire completion | PASS | D-T6-002 (500 + GET fallback; 69 controls) |
| H13 Evidence curation | FAIL | D-T6-005 (pre-commit 500; 0 observations committed) |
| H14 Report generation | PASS | D-T6-002 (500 + GET fallback) |
| H15 Report QA | FAIL | D-T6-006 (qa-approve 500; structural QA passed) |
| H16 Executive PDF export | PASS | — |
| H17 CG v0 drift-cycle rehearsal | FAIL | D-T6-003 (no baseline; no re-scan) |
| H18 Access revocation | PASS | — |

---

## Defects Found

| # | Severity | Description | Resolution |
|---|---|---|---|
| D-T6-001 | Low | `FG_DB_MIGRATIONS_RISK_ACCEPTED=1` stale in Railway | Clear Railway var post-T6 |
| D-T6-002 | Medium | Post-commit ORM expiry in engagement/questionnaire/report routes; data commits but response 500 | Capture attrs before commit or re-query; engineering buffer |
| D-T6-003 | High | Post-commit `ObjectDeletedError` in scan initiation routes; background tasks never scheduled | Same fix as D-T6-002; engineering buffer |
| D-T6-004 | High | Portal invitation email missing `?tenant_id`; BFF falls back to wrong tenant env var | Add `?tenant_id={tenant_id}` to invitation email template |
| D-T6-005 | High | Pre-commit 500 in `capture_observation_route`; 0 observations committed | Investigate Railway log; engineering buffer |
| D-T6-006 | Medium | Post-commit ORM expiry in `qa_approve_report_route` response serialization; approval likely committed | Same fix as D-T6-002; engineering buffer |

---

## Actions Required Before Second Run

1. **D-T6-001:** Clear `FG_DB_MIGRATIONS_RISK_ACCEPTED` Railway variable.
2. **D-T6-002 / D-T6-003 / D-T6-006:** Fix post-commit ORM expiry pattern across `create_engagement_route`, `capture_observation_route`, `create_or_get_questionnaire`, `create_engagement_report_route`, `qa_approve_report_route`, and all scan initiation routes. Fix: capture IDs/status values before `db.commit()`, or set `expire_on_commit=False` on affected sessions, or re-set RLS context and re-query after commit.
3. **D-T6-004:** Update portal invitation email template to include `?tenant_id={tenant_id}` in the accept URL.
4. **D-T6-005:** Inspect Railway logs for T6 observation POST 500 stack trace; fix pre-commit failure in `capture_observation_route`.
5. **Second run:** Re-run full H1–H18 or at minimum H5, H10–H11, H13, H15, H17 after fixes confirmed in staging.

---

## CG v0 Drift-Cycle Status

Not produced. H17 (drift-cycle rehearsal) failed due to D-T6-003 — no scan baseline exists. Deferred to T6 second run.

---

## Evidence

Full evidence: `docs/governance/status/T6_OPERATIONAL_REHEARSAL_EVIDENCE.md`
Artifacts: `/tmp/t6_evidence/` (local; not committed — contains API responses without secrets)
