name: fg-required

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: fg-required-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  fg-required:
    name: fg-required
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: setup python + cache pip
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt
            constraints.txt
            pyproject.toml

      - name: verify workflow filename uniqueness
        run: |
          test -f .github/workflows/fg-required.yml
          test ! -f .github/workflows/fg-required.yaml
          test ! -f .github/workflows/FG-required.yml

      - name: run hermetic tool tests
        # tools_minimal intentionally uses unittest to avoid pytest bootstrap/plugin imports
        run: python -m unittest -q tests.tools_minimal.test_fg_required_minimal

      - name: create venv
        run: python -m venv .venv

      - name: install deps (pinned)
        run: make venv

      - name: sanity check pytest-asyncio plugin version
        run: .venv/bin/python -c "import pytest_asyncio; print(pytest_asyncio.__version__)"

      - name: run required-tests gate resolution tests
        run: .venv/bin/python -m unittest -q tests.tools_minimal.test_required_tests_gate_resolution

      - name: run fg-required
        id: fg_required
        timeout-minutes: 8
        run: .venv/bin/python tools/testing/harness/fg_required.py --global-budget-seconds 480 --lane-timeout-seconds 480

      - name: prepare artifacts dirs + fallback notice
        if: always()
        run: |
          mkdir -p artifacts/testing artifacts/fg-required

          # Ensure we always have at least one file to upload, even if fg-required crashes early.
          test -f artifacts/testing/_upload_notice.txt || \
            echo "fg-required artifact bundle prepared after failure" > artifacts/testing/_upload_notice.txt

          test -f artifacts/fg-required/_upload_notice.txt || \
            echo "fg-required artifact bundle prepared after failure" > artifacts/fg-required/_upload_notice.txt

      - name: verify required artifacts
        # Only enforce existence when fg-required actually succeeded.
        if: success()
        run: |
          test -f artifacts/testing/fg-required-summary.json
          test -f artifacts/testing/fg-required-summary.md

      - name: upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fg-required-artifacts
          path: |
            artifacts/fg-required/**
            artifacts/gates/**
            artifacts/docker/**
            artifacts/testing/**
            artifacts/**/_upload_notice.txt
          if-no-files-found: warn
          retention-days: 7