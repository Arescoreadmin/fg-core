[pytest]
testpaths =
    tests
    backend/tests
norecursedirs =
    .venv
    artifacts
    state

# pytest-asyncio: kill the deprecation warning properly

markers =
    smoke: quick sanity checks
    contract: API/UI contract checks
    e2e_http: HTTP e2e tests against a running server
    integration: hits real running server
    asyncio: async tests
    postgres: requires Postgres (FG_DB_BACKEND=postgres + FG_DB_URL)

# =============================================================================
# Filter Warnings
# =============================================================================
filterwarnings =
    error
